# 1.5.3

修一条实机报错：点「✨ AI 补充」会失败，界面上显示 **`AI 补充失败：finish=max-tokens`**，而且
模型其实已经写出了一条可用的提交信息、却被整段丢弃。根因不在模型、也不在上下文（输入上限没有
失守），而是**我们把"模型用完了输出预算"和"请求失败"当成了同一件事**：旧代码是
`if (finish?.kind !== 'stop') throw …`，`max-tokens` 因此变成硬失败，连已经生成的文本一起丢掉，
并把内部的 `finish=max-tokens` 原文端给了用户。

## 修复

- **输出预算 400 → 1024。** 一条提交信息本身只有几十个 token，但这条请求走的是**用户当前的
  默认模型**，部分模型会先花掉一段推理 token（`reasoningTokens` 计入输出预算），400 对辅助型
  生成过于紧张。1024 是"够用且仍然很短"的量级（标题 + 三条要点约 100~200 token），刻意没有抬到
  几千上万——提交信息仍然应该是短输出。
- **`max-tokens` 不再等于失败。** 判断顺序固定为 **`blocks()` → 规范化 → 再看 finish**（绝不
  在取文本之前判 finish）：
  - `finish = stop`：正常返回；文本为空才报 `aiEmpty`；
  - `finish = max-tokens` 且**已有可用文本**（有标题，或至少有一条要点——截断恰好落在标题之后时
    那份要点列表仍然能直接贴进提交框）：**照常返回**，并带上 `truncated: true` 与
    `finishReason: 'max-tokens'`；
  - `finish = max-tokens` 且一个字都没留下：才报 `aiOutputLimit`，界面显示本语言的
    「AI 生成内容超过长度限制，请重试。」——`finish=max-tokens` 这种内部原因**不再**出现在用户
    可见的文案里；
  - 带 `failure` 的 `error` / `aborted`（认证、provider、超时、取消）仍然走 `describeLlmFailure`
    的失败路径：**认证失败绝不允许因为"恰好吐了几个字"而被伪装成成功**。
- **系统指令从源头限长**（与输出预算配套，避免只改一边）：恰好一行简洁标题、最多 3 条要点、
  最多 8 行、最多 500 字符、不要解释推理、不要分析与前言，中英文都遵守。
- **界面提示分开**：截断但成功时正常填入输入框，只给一条非阻塞提示「AI 输出达到长度上限，已保留
  生成的提交信息。」（不是红色失败）；只有真正的失败才显示失败提示。已有用户输入时的
  「替换 / 追加 / 取消」三选一保持不变，采用截断建议后同样给那句提示。
- **输入侧的上限一个都没动**（30 文件 / 单文件 3000 字符 / 总共 30000 字符）：这次是**输出**
  预算问题，不是输入上下文溢出，没有为了修它放宽任何输入约束。

## 校验

- `scripts/test-review-commit-message.mjs` 从 60 项扩到 **97 项**，新增第 6/7 节完整覆盖这份
  语义：A `stop` + 文本 → 成功；B `max-tokens` + 文本 → 成功、`message` 正常、`truncated:true`、
  `finishReason:'max-tokens'`；C `max-tokens` + 标题与要点 → 已有文本原样保留（含"只有要点"与
  "多块文本按顺序拼接"）；D `max-tokens` + 空文本 / 只有空白 → `aiOutputLimit`（且文案里没有
  `finish=`）；E `failure{code:'AUTH'}` 即使已有部分文本也必须失败（`aborted` 同理、无 code 兜底
  成 `aiFailed`）；F 请求里的 `maxTokens === 1024`；另有 `stop` + 空文本 → `aiEmpty`，以及系统
  指令里那 7 条输出约束真的进了 `system` 槽位。
- `scripts/test-review-staging.mjs` 新增两条界面断言（共 **228 项**）：截断但成功时内容照常填入、
  提示是「达到长度上限」而**不是** `aiCommitFailed`、且文案里没有 `finish=`；`aiOutputLimit` 走
  本语言短句。
- `scripts/mutation-check.mjs` 扩到 **37 项**，新增 4 项：把 `finish !== stop` 重新当成硬失败、
  把输出预算退回 400、去掉系统指令里的长度约束、让 `failure` 也走"有文本即成功"——每一项都确认
  "改回旧写法即变红、还原即变绿"。
- **真实模型 smoke**（新增 `scripts/probe-commit-message.mjs`，隔离 home + 隔离仓库，凭据只从
  进程环境注入）：同一台机器、同一条仓库、同一个模型（`deepseek-official/deepseek-flash`）实测——
  - 当前实现（预算 1024）：`finishReason=stop`、`truncated=false`，5 行 / 178 字符，1 条标题 +
    3 条要点，1.5~2.0 s；
  - 把预算压到 40（新语义）：`finishReason=max-tokens`、`truncated=true`，**已生成的标题与 2 条
    要点被保留**（131 字符，最后一条要点明显被截断）；
  - 同样的输入 + 旧的"非 stop 即抛错"语义：**HTTP 502 / `code=aiFailed` / `detail=finish=max-tokens`**
    ——实机那条报错被逐字复现；
  - 顺带说明：在 `deepseek-flash` + 3 个文件的小改动上，**400 token 并没有触发截断**（同样是
    `stop`）。因此"400 太紧"是**必要条件而非充分条件**：真正把这条 latent bug 引爆的是"某些模型
    的推理 token 也算进输出预算"以及更大的改动集，而修复让两条路都不再致命。
- 离线全量 **26 个脚本 / 2,120 项断言 0 失败**（唯一失败仍是既有的 `test-unpack.mjs`，检查的是
  陈旧归档）；`tsc --noEmit`、`check-imports`、`check-plugin-i18n`、`check-readme`（中英）、
  `test-i18n` 全部通过。

---

# 1.5.2

这一版把**项目级 Git 的作用域**与**未跟踪文件的规模问题**一起解决掉，并顺手修掉分支弹窗二级菜单的
两个交互缺陷。起因是两个真实形状：一是**工作区常常只是仓库的一个子目录**（例如
`D:/project/mmsm-amis/pages/mse`），而项目面板此前把"用户打开的目录"同时当成 Git 的 cwd、路径基准、
快照缓存键与仓库身份——于是同一个仓库的不同子目录各跑一套 10 秒轮询、`ls-files --others` 因为带
cwd 前缀而少报文件、路径时而 `../src/a.js` 时而 `src/a.js`；二是**一个仓库里有 6,846 个未跟踪文件**
（一个没有被 `.gitignore` 覆盖的 `tmp/`），常驻轮询每次都在搬几千条路径，而界面只列前 50 条再加一句
"还有 6,796 个未显示"——既不是完整列表也不是概要。这一版把这两件事都按"有界"重做：**Git 工作量与
未跟踪文件数量脱钩**，路径基准唯一，且同一仓库只跑一套轮询。

## 变更

- **正式拆分 `workspaceRoot` 与 `repositoryRoot`（host 推导，客户端不可信）。** 新增
  `lib/repo-context.js`（review 与 gitbar 各持一份**逐字节相同**的副本）：以
  `git rev-parse --show-toplevel --absolute-git-dir` **一次调用**推导仓库根，配上有界缓存
  （最多 64 条、正向 TTL 5 分钟、负向 10 秒）与 single-flight。`repositoryRoot` 永远由 host 推导，
  客户端只能送 `workspaceRoot`——否则 `repositoryRoot=C:/` 就能越过工作区安全边界。
  - **所有 Git 路由都以仓库根为 cwd**：`status` / `diff` / `workspace-file` / `stage` / `unstage` /
    `revert` / `commit` / `commit-and-push` / `file-history` / `graph` / `commit-detail` /
    `commit-file` / `history` / `untracked`。这同时修掉一个真实缺陷：合并/变基的标记
    （`.git/MERGE_HEAD`）以前按"工作区/.git/…"找，工作区是子目录时那个路径根本不存在，
    于是"正在进行合并"永远显示不出来。
  - **路径统一为仓库相对**：`repo/src` 作为工作区时，`Changes` 照样显示 `root.txt` /
    `src/a.js` / `src/deep/b.js` / `pages/x.json`——git 自己就是这样（`status` 与 `diff` 的输出本就是
    仓库相对），因此这一版**没有**再加 `path.startsWith(workspaceRoot)` 之类的过滤。
  - 预留 `gitScope: { workspaceRoot, repositoryRoot, repositories: [...] }`：本轮长度恒为 1，将来
    `project/{frontend,backend}` 各带一个 `.git` 时不用改数据形状（也**不**递归扫硬盘找 `.git`）。
- **同一个 `repositoryRoot` 只共享一份快照、一套轮询。** 客户端快照 store 的键从
  `workspaceRoot` 改成**仓库根**（`repo/src` 与 `repo/pages` 因此落在同一条记录上），组件仍按工作区
  订阅。于是"切一个子目录"不再清空分支/Changes/徽标、不再重新全量扫描、不再多一套轮询；两个工作区
  的订阅者挂在同一条记录、同一个在途请求上。临时索引与基线也改成按仓库记（同仓库只付一次全量代价）。
- **常驻快照走快路径。** `/workspace` 与 `/status` 改用
  `status --porcelain=v2 --branch -z --untracked-files=normal`：git 会把整块未跟踪目录折叠成一条
  `tmp/`，因此 6,846 个未跟踪文件在轮询里只是 1~2 条记录。实测同一个夹具：
  `-uall` **174.4 KB / 6,855 条记录** → `-unormal` **0.6 KB / 10 条记录**；
  `/workspace`（6,848 个未跟踪）**96 ms / 1.6 KB**，且**不向渲染进程传任何一条未跟踪路径**。
  `changedFiles` 是精确值时带 `changedFilesExact: true`，折叠状态下明确标成估计值。
- **未跟踪文件改成 IDEA 式双模式**（阈值 `UNTRACKED_INLINE_LIMIT = 50`）：
  - **≤ 50（inline）**：主面板逐行列出**全部**未跟踪文件（勾选、加入 Git、查看差异、变更记录都在），
    并在有界预算内补上精确 `+N`（单文件 ≤ 1 MB、一轮总量 ≤ 8 MB、二进制只标记不读全文，
    **绝不为一个数字 fork 一个 git 进程**）；
  - **> 50（browse）**：主面板**一行都不列**，只显示"未进行版本管理的文件 6,846 个文件 [浏览]"。
    DOM 里既没有 6,846 行，也没有"前 50 行 + 还有 N 个"那种半成品；
  - **还没统计出来（pending）**：显示"正在统计未跟踪文件…"，并**按需**触发一次精确枚举。
- **精确枚举改成 lazy + 按仓库缓存。** 只有三种情况才做完整枚举：Changes 页签要判定 inline/browse、
  用户点「浏览」、写操作之后（缓存被 invalidate）。路由 `POST /review/untracked` 一次返回精确条数 +
  模式 + 该层的直接子节点，缓存 TTL 20 秒、按 `repositoryRoot` 共享（`repo/src → repo/pages` 直接
  复用）。实测 6,848 条的精确枚举 **115 ms / 1.2 KB**。
- **「浏览未进行版本管理的文件」弹窗（惰性树）。** 目录默认折叠，只请求当前前缀的**直接子节点**；
  同级节点分页（一页 200 条 + 「继续加载」），因此展开一个躺着 6,835 个文件的目录不会一次 mount
  几千行；文件与目录都能勾（目录勾上 = 整棵子树，把**目录路径**交给 `git add`），部分勾选是
  indeterminate，默认全不选，顶部有「全选 / 清空」。加入 Git 之后**就地刷新**已展开的层并清空选择，
  主面板同时从 browse 变回 inline（`51 → 加 3 → 48` 这条路径有专门的断言）。
- **大批量 `git add` 有界。** 一次加入几千个未跟踪文件时不再拼超长命令行，而是写一个 NUL 分隔的
  临时 pathspec 文件、用 `git add --pathspec-from-file=… --pathspec-file-nul`（临时文件在 `finally`
  里删掉）；老 git 不认这个选项时退回**双上限**批处理（路径数与总 argv 字符数一起限，Windows 命令行
  上限 32K）。「全选 → 加入 Git」走 `{ all: 'untracked' }`：由 host 用**它手上那份**完整清单去 add，
  渲染进程永远不持有那几千条路径。实测一次 add 6,835 个路径 **4.4 s**，索引里确实多了 6,835 条。
- **分支弹窗的二级菜单改成 IDEA 式外侧级联，并消灭孤儿菜单。**
  - 二级菜单不再从分支行左边缘开始（那会盖住一级面板），而是优先出现在**一级面板右侧**
    （`left = panelRect.right + gap`），右侧空间不足时出现在左侧；两边都放不下才在视口内做兜底。
    位置由 `{ branch, rowAnchor, panelAnchor }` 两个矩形算出来，纵向按行对齐并在必要时上翻，
    首帧用估算高度、挂载后用真实高度微调（不闪）。
  - **孤儿菜单的根因**：二级菜单的渲染条件只看 `menu !== null`，而"点面板外"只执行
    `setOpen(false)`——既不收二级菜单也不清 200 ms 的单击定时器。于是"面板已消失、菜单还挂着"，
    或者"点一下别处之后 200 ms 菜单自己冒出来"。现在统一成
    `closePanel()`（面板 + 二级 + 对话框 + 定时器一起收）与 `closeBranchMenu()`（只收二级 + 定时器），
    所有关面板路径都走它们，并在渲染层再加一道 `open === true` 的闸门。
- **`gitbar` 与 `review` 共用同一套仓库解析**：同一个工作区的分支徽章、`Changes`、`Log`、
  `stage`、`commit`、`checkout` 因此操作**同一个仓库根**（两份副本由逐字节比较 + 真实临时仓库的
  解析对比钉住）。gitbar 的响应也带上 `workspaceRoot` / `repositoryRoot` / `gitScope`。

## 校验

- 新增 `scripts/test-review-repo-scope.mjs`（**110 项断言**，真实仓库 + 真实 HTTP + **6,846 个未跟踪
  文件**的夹具）：作用域推导与"客户端传来的 repositoryRoot 被忽略"、子目录工作区列出全仓库四条改动、
  快路径不搬路径（响应 < 8 KB、`-unormal` 与 `-uall` 的体量对照）、精确枚举与按仓库复用、50/51 阈值、
  惰性树 + 分页 + 404、同仓库两个工作区共用缓存、6835 个路径一次 add（pathspec-from-file + 临时文件
  清理 + 之后自动变 inline）、未跟踪文件按需差异。
- `scripts/test-review-staging.mjs` 新增第 13/14 节（**222 项断言**）：inline 逐行 / browse 只给摘要 +
  浏览入口（主面板 0 行）/ pending 显示"正在统计…"并只发一次精确枚举（在途期间不重复发）、浏览弹窗的
  惰性树与分页与勾选与"全选走 all: untracked"、**同一仓库两个工作区只有一格记录、一套轮询**（并断言
  退订后停止轮询、换工作区不清空、僵尸记录不会残留）。
- `scripts/test-gitbar-branch-interaction.mjs` 扩到 **100 项断言**（新增级联几何 `S.left ≥ P.right + gap`
  / 左侧回退 / 点外两级全关 / 点一级内只关二级 / `open === false` 时二级计数为 0，以及孤儿菜单的六条
  回归与 Esc 三层顺序）；`test-gitbar-branch-perf.mjs` 新增"仓库探针只跑一次"的断言（第二次
  `/branches` 只起 1 个 git 进程）。
- `scripts/test-review-graph.mjs` / `test-review-host.mjs` 更新为新的快照契约（未跟踪不再混进
  `files`、`untracked` 是摘要对象、`/untracked` 是惰性树）；四个 spawn 运行时的 HTTP 测试现在**自己
  先同步插件**——此前它们会在 `runtime/node_modules` 里那份旧副本上跑并"全绿"，实测踩到过。
- `scripts/mutation-check.mjs` 扩到 **33 项**，新增 11 项针对这一版：未跟踪大量时仍逐行列出、把摘要当
  裸数字、精确枚举读错字段、store 退回按工作区建格、合并时留下僵尸记录、枚举退回 `-uall`、把未跟踪
  塞回 `files`、大批量 add 退回单参数、所有命令退回工作区、惰性树不返回 404、二级菜单不优先右侧
  ——每一项都确认"改回旧写法即变红、还原即变绿"。
- 离线全量：**26 个脚本 / 2,077 项断言 0 失败**（唯一失败的是既有的 `test-unpack.mjs`，它检查的是
  陈旧的 `build/runtime.br` 归档，与本次改动无关）；`tsc --noEmit`、`check-imports`、
  `check-react-rules`、`check-plugin-i18n`（两个插件）、`check-readme`（中英）、`test-i18n`、
  `test-ui-typography` 全部通过。
- CDP/Electron 冒烟（需要活体实例，请在桌面会话里跑）：`test-gitbar-ui.mjs` / `test-menu-anchor.mjs`
  可验证二级菜单的真实像素几何；`test-review-sidebar.mjs` / `test-project-git-smoke.mjs` 覆盖面板本身。

---

# 1.5.1

这一版来自两条实机反馈：**`Log` 底部 Diff Preview 的长代码不会自动换行**（Go / Java / SQL 一行几百
字符，方向键外的半个句子必须拖横向滚动条才看得到），以及 **`Changes` 页签点文件之后差异直接 inline
插进文件列表**（视觉上像是那一行"长出了"一块代码，左右两件事——挑要提交哪些文件 vs. 看这个文件改了
什么——挤在一列里，既不好读也不好点）。因此这一版做两件事：**统一 `Log` 与 `Changes` 的 Diff
Viewer**，并**重构 `Changes` 的预览布局**为左右两栏。不是为了改样式而改样式：两个页签的差异从此是
同一份实现，行为与外观不会再分叉。

## 变更

- **默认自动换行，并给一个开关。** 差异正文的代码格改成 `white-space: pre-wrap` +
  `overflow-wrap: anywhere` + `word-break: break-word`（**绝不使用 `normal`**——那会把代码缩进、
  括号对齐全部吃掉），tab 按 4 展开；长行折进可视宽度，`https://…` 之类的"没有空白可折"的超长词
  与 minified 内容也不会撑破容器。换行态下正文容器是 `overflow-x: hidden`：文字已经折好了，底下再挂
  一条没用的横向滚动条是纯噪音（这正是实机反馈里的观感问题）。
  - 头部右侧新增「自动换行」图标按钮（`aria-pressed` + tooltip）。关掉它回到 `white-space: pre`、
    `overflow-x: auto`，需要看原始横向结构时仍可切换。
  - 偏好持久化在 `dsh.review.diffWrap`，**`Log` 与 `Changes` 共用同一份**：在 `Log` 里关掉，
    `Changes` 也不换行。默认开启。
- **换行之后行号不再重复。** 行仍然是 `旧行号 | 新行号 | +/− | 代码` 四列，但它是一个**网格**：
  行号（两格）、增删标记（一格）、代码（`minmax(0, 1fr)`）各占固定的列。一条逻辑行折成三个视觉行
  时，行号与 `+` / `−` 只出现一次、并且停在第一行（`align-items: start`），整行底色覆盖全部续行。
- **`Log` 与 `Changes` 合并到唯一的 `ReviewDiffViewer`。** 头部（状态徽标 / 目录压暗 + 文件名 /
  `+N −M` / 自动换行 / 关闭）、逐行正文、行号栏、hunk 头、折叠后的文件头、以及加载 / 失败 / 二进制
  / 截断四种状态只有**一处实现**；`Log` 的 Diff Preview 与会话内文件列表的 `FileDiff`、
  `LazyFileDiff` 都委托给它。以前"两套差异视图各改一半"的双轨结构到此结束。
- **`Changes` 改成左右两栏**（需求一句话："左边=我要提交哪些文件 / 右边=当前文件具体改了什么 /
  底部=提交信息"）：
  - 左栏仍是三组（已暂存 / 更改 / 未跟踪）与全部原有动作（勾选、全选、暂存、取消暂存、还原、变更
    记录、加入 git），**点文件名只改变选中**——不碰勾选状态、不碰暂存态、不影响 `commitPaths`；
    选中的行有 `aria-selected` / `data-staging-selected` 与一层浅强调色。
  - 右栏是差异预览，头部简化成 `M pages/…/business-agreement-approval.json +16 −16` 加右侧的
    「自动换行」「关闭」两个按钮，不再重复一张文件卡片。点**关闭**即取消选中（行高亮消失、右栏回到
    空态），再点同一个文件从缓存恢复、不重新取数。
  - 两栏之间是可拖动的分隔条（`role="separator"`）：默认左栏 **34%**，范围 280px ~ 可用宽度的
    50%，双击复位，持久化在 `dsh.review.changesFileWidth`；左右两栏各自滚动。
  - **窄窗口（可用宽度 < 900px）退化成上下堆叠**（文件列表 ── 差异 ── 提交区），任何情况下都不会
    退回"在文件行下面 inline 展开"。
  - 提交区仍然固定贴底：`main` 是 `flex: 1 1 auto; min-height: 0`，提交卡片 `flex-shrink: 0` 且在
    main **之外**（`order: 3`），因此差异再长也不会把它挤出可视区。

## 修复

- **`Log` 底部 Diff Preview 的长代码不再需要横向拖动。** 默认换行之后，Go / Java / JSON / SQL 的
  长行一眼可读；关掉换行仍可看原始横向结构。
- **`Changes` 里点文件不再把差异插进文件列表。** 差异只在右栏渲染：左栏的高度只取决于文件数，
  右栏就地换内容，不会再出现"文件列表被某一行撑长、跟着一起滚动"。
- **换行不会把行号画重。** 旧实现若用"续行也输出行号"的写法会出现 12 12 12 这种重复，现在由网格
  列结构天然保证一条逻辑行只有一份行号与标记。

## 校验

- `scripts/test-review-graph-view.mjs`：第 11f 节钉住默认换行（`overflowX: hidden`、`wrap=on`、
  `pre-wrap` / `anywhere` / `break-word` / tabSize 4、行号分列 1/2、标记列 3、代码列以
  `minmax(0, 1fr)` 收尾且 `align-items: start`）；新增 11f2（关掉 → `auto` + `pre` + 落盘 `'0'`，
  再打开 → `'1'`，**重挂后偏好仍然生效**）与 11f3（一条逻辑行只有一个行号/标记/代码格）。
- `scripts/test-review-overlay-hooks.mjs`：第 6b 节钉"提交区在 main 之外、`order: 3`、
  `flex-shrink: 0`"；6b2 钉左右两栏 / 分隔条 / 未选文件时的空态提示 / 左栏里没有任何差异行；
  6b3 钉"点文件只改选中"（整份勾选映射前后一致、差异行只出现在右栏之后、换文件原地切换）；
  新增 7b 钉**换行偏好在 `Log` 与 `Changes` 之间共用**（一边关掉另一边也不换行、落盘、再打开）
  以及两侧差异字号的 uiPx 派生。
- `scripts/test-review-staging.mjs`、`test-review-lazy-diff.mjs` 更新为"选中 → 右栏"语义：关闭即
  取消选中、再点走缓存、迟到的 A 响应不许覆盖 B、换项目/换 HEAD 重新取。
- `scripts/mutation-check.mjs` 扩到 **22 项**，其中把 `pre-wrap` 偷偷改回 `pre`、让查看器忽略共享
  偏好、换行偏好不落盘、把差异 inline 塞回 `Changes` 文件行、关闭时不取消选中、点文件名顺手改勾选
  ——每一项都确认"改回旧写法即变红、还原即变绿"。
- 真实 Electron/CDP 冒烟（需带远程调试端口的桌面会话）更新为断言默认 `white-space: pre-wrap` /
  `overflow-x: hidden`、点「自动换行」后变 `pre` / `auto`、旧新行号各占一列，并新增 **G3**：Changes
  左右两栏、左栏内差异行为 0、选中标记、勾选状态不受影响、整屏只有一个 Viewer、提交区贴底。

---

# 1.5.0

上一版把功能做通了，但 `Log` 页签的**观感**是这一版要解决的问题：完整的代码 diff 被内联展开在
右侧只有 320~420px 的详情栏里，结果代码区太窄、行号与增删列与正文互相挤压、Go / Java 的长代码
几乎不可读、每行都要横向滚动。这一版**重构了 Diff 的布局**——差异不再塞进窄右栏，而是移到
Log 底部一块**横跨"提交图 + 详情"的 Diff Preview**；同时把差异本身的排版、配色与字号按"读代码"
重新定过。纯粹的"把字号调小"没有采用。

## 变更

- **新增底部 Diff Preview（横跨提交图 + 详情）。** 右栏从此只负责 `CommitSummary`、改动文件数量
  与 `CommitFileList`；点一个改动文件**不再在行下方内联展开**，而是把 `{ revision, path }` 交给
  下面的宽栏。左侧分支树**不参与**这次纵向切分，因此不管 Preview 多高它都不会被压扁。
  - 默认高度是 Log 可用高度的 **40%**（需求给的 38%~45% 区间内），可上下拖动 splitter 调整，
    双击 splitter 复位回默认；拖动过的高度按应用持久化在 `dsh.review.graphDiffHeight`。
  - `×`、Escape、工具栏上的"显示/隐藏 Diff Preview"都能收起它；收起**只收起、不丢选中**，
    再点同一个文件或按工具栏按钮即原样恢复。Escape 挂在**捕获阶段**并 `stopPropagation`，
    因此它只收代码区、不会把整个抽屉关掉（输入框里的 Escape 仍然归输入框）。
  - 窄窗口退化时优先保住**提交图与 Diff Preview**，分支树与详情继续可以收起。
- **差异正文重新排版。** 固定三段 `旧行号 | 新行号 | +/− | 代码`：行号栏固定宽度、右对齐、
  单独底色 + 右侧描边、`user-select: none`；正文当时是 `white-space: pre` 不折行（**1.5.1 已改为
  默认自动换行 + 可切换，见上**——折行本身没错，错的是一行几百字符却只能横向拖）；横向滚动发生在
  Preview 的正文容器上，每一行自己不产生滚动条；行高从 1.55 收到 **1.45**，一屏能多放几行。
- **配色改成"增删是背景、代码是主体"。** 以前新增区是一大块高饱和绿色、文字也偏绿，视觉很重。
  现在新增/删除行只用约 **9% 的浅绿/浅红混色**作底，正文保持普通代码文字色；饱和色只留给
  `+` / `−` 标记与行号栏。深浅两套主题都按这个规则重新配过。
- **hunk 头与文件头分层。** `@@ -357,6 +357,97 @@` 单独一行、用更小的字号、带淡蓝底与内边距，
  与普通代码行区分开；git 的 `diff --git` / `index` / `--- a/…` / `+++ b/…` **默认折叠成一条**
  `File changed`（新建/删除/重命名分别说成 `New file` / `Deleted file` / `Renamed file`，
  原始若干行保留在它的 title 里以便追溯）。用户要看的是"改在哪儿、改了什么"，不是 patch 元数据。
- **字号按用途分层，而不是机械沿用 UI 字号。** 新增语义 token `reviewFont`：提交标题 12.5、
  次级信息（作者/时间/计数）11.5、改动文件行 11.5、**差异正文 11**、行号与 hunk 头 10.5。
  每一层仍然由 `uiPx()` 从 UI 字号派生，因此"设置 → UI 字号"调到 12 / 18 时整体同比缩放，
  不会有人漏接。
- **右侧详情栏不再需要承担代码展示，因此宽度策略调整**：默认从 320 提到 **340**（仍然在
  320~360 区间），上限**不再死卡 420**，允许用户主动向左拖到 `min(600, 视口 × 0.4)`——
  长文件名与较长的 commit body 因此舒展得开。
- 改动文件行本身保持紧凑：仍然一行一个文件（状态徽标 + 目录压暗 + 文件名突出 + `+N −M`），
  选中的文件加一层强调色底并对齐下面的 Preview（`aria-selected` 取代了原来的 `aria-expanded`，
  因为语义从"展开/收起"变成了"下面正在看哪一个文件"）。

## 修复

- **`Log` 里点改动文件不再把右栏撑成一条超长滚动页。** 右栏的文件清单高度只取决于文件数，
  与是否看过 diff 无关。
- **快速切文件不会串内容。** 先点 A（请求慢）再点 B，A 的迟到响应**不允许**覆盖 B：请求带令牌，
  落地时既要令牌匹配、又要缓存键仍等于当前请求的键；再次点击同一文件不重复请求（模块级缓存
  `workspace + revision + path`，另有 in-flight 去重）。
- **切提交会清空 Preview。** 上一条提交的差异**在切换的那一帧就不显示**——除了 effect 清状态，
  渲染时还会再过滤一次"这份选中是否属于当前提交"（只靠 effect 的话那一帧仍会画出旧预览，
  同一路径时几乎看不出来）。缓存键里本来就带 revision，因此也不会命中旧提交那份内容。

## 校验

- `scripts/test-review-graph-view.mjs`（**218 项**断言，本次新增 60+）：第 1 条钉"右栏里不存在
  内联差异子树"；点击文件后请求只带 `{workspace, revision, path}`（**不带整条提交的文件清单**）、
  Preview 横跨"提交图 + 详情"且不在右栏里；再次点击保持选中且不重复请求；**A/B 竞态**（用挂起
  的 A 响应验证迟到响应不覆盖 B）；关闭后三栏不受影响、选中仍在、重开走缓存；**切提交的同帧与
  稳定后都没有旧预览**、新提交里同一路径会重新请求；**高度拖动 → 变高 → 持久化 → 双击复位清掉
  持久化**；Escape 收代码区且**不传给抽屉**、输入框里的 Escape 不被抢；工具栏开关；差异正文字号
  /不折行/行号栏固定宽度与描边/hunk 头更小字号/新增行正文用普通文字色而底色是低比例混色/
  文件头折叠成一条；收起分支树与详情后图形与 Preview 仍在。（其中"不折行"一条在 **1.5.1**
  被反过来钉成"默认 `pre-wrap`"，当时的断言见 1.5.1 的校验一节。）
- `scripts/test-review-overlay-hooks.mjs`（**126 项**）：右栏"不取差异、不渲染差异"，点文件后
  差异只在 Preview 里、右栏里一行差异都没有；选中文件行有 `aria-selected`；关闭按钮与三栏。
- `scripts/test-review-lazy-diff.mjs`、`test-review-workspace-race.mjs` 等随之更新（右栏默认 340、
  差异正文是多了一层"横向滚动容器"的组件，断言改为展开子树取文本）。
- `scripts/mutation-check.mjs` 扩到 **17 项**（1.5.1 起为 **22 项**），新增 5 项针对这一版：把差异
  重新塞回右栏、让正文变回折行、高度不持久化、切提交不清空选中、文件头不再折叠——每一项都确认
  "改回旧写法即变红、还原即变绿"。
- 离线全量：**26 个脚本 / 1842 项断言 0 失败**；`check-react-rules`、`check-plugin-i18n`、
  `check-readme`（中英）、类型检查全部通过。
- 真实 Electron/CDP 冒烟新增 **G2**：点改动文件出现 `data-graph-diff-preview`、**右栏内差异行为
  0**、行号栏不可选中、文件头已折叠、默认高度是 `0 0 40%`、关闭后三栏仍在且再点同一文件立即恢复，
  并检查这一段没有 React error / ReferenceError（代码区当时断言 `white-space: pre` + `overflow-x:
  auto`；**1.5.1 已反转为默认 `pre-wrap` + `overflow-x: hidden` 并新增开关断言**。该脚本需要带远程
  调试端口的实例，请在桌面会话里跑）。

---

# 1.4.9

这一版把"项目级 Git 抽屉"按实际使用反馈收口：**点抽屉外任何地方都会关掉它**（上一版为了让
切项目时它不被关掉而做了豁免，结果变成"想回主界面必须先精确找到 X"）；未跟踪文件点击查看在
上一版是**直接报错**的（按需差异改造删掉了它仍在引用的那个变量）；提交图不再显示哈希、时间
精确到秒；滚到底会真的继续加载，而**左栏不会在滚动中自己变内容**；字号跟随"设置 → UI 字号"；
并新增**「✨ AI 补充」**——按你勾选的文件草拟提交信息，走的是宿主自己的模型能力。

## 修复

- **未跟踪文件点击查看直接报错。** `Changes` 页签里点一个未跟踪文件的路径，面板会立刻报
  `ReferenceError: byFile is not defined`。根因是上一版把"整页统一差异"改成按需取（`/workspace-file`
  + `LazyFileDiff`）时删掉了 `StagingSection` 里的 `byFile`，而未跟踪分支仍在传
  `diff: byFile.get(path) ?? ''`。现在未跟踪文件与已跟踪文件走**同一条**按需路径
  （`LazyFileDiff` + `untracked: true`，host 侧用 `git diff --no-index /dev/null <path>`），
  缓存键同样是 `workspace + HEAD + 路径`，因此切项目、提交后旧差异都会失效，收起再展开走缓存。
- **点抽屉外不关。** 现在点聊天正文、点左侧列表、点任何空白都关闭；Escape 与 X 继续关闭。
  反向的"不能误关"逐条豁免：抽屉内部（含它自己的浮层与确认框）、分支右键菜单与分支面板
  （它们由 gitbar 渲染在 body 级，不在抽屉的 DOM 子树里）、以及右上角入口按钮。入口**必须**
  豁免——捕获阶段的 `mousedown` 会先把它关掉、紧接着按钮自己的点击又打开，用户看到的是
  "闪一下、打不开"。顺带修掉一个真实崩溃：程序化派发的 `mousedown` 没有 `target` 时，
  `typeof undefined.closest` 会抛 TypeError，一次点击就把整个抽屉带走。
- **提交图不再显示哈希。** 中栏每一行与右栏摘要都去掉了 SHA（`hash`/`short` 内部全部保留：
  React key、选中身份、详情请求、泳道布局、缓存键都还依赖它）。少掉的那一列宽度让提交标题
  不再被挤成省略号。
- **提交时间只到分钟。** 同一分钟内的连续提交分不出先后。现在统一走 `formatCommitTime`，
  中栏与右栏都精确到秒（`2026-09-21 15:42:18`），且**只取 ISO 字符串本身的年月日时分秒**、
  不做时区换算（提交时间是历史事实，走 `new Date()` 会让同一条提交在不同机器上显示不同时间）。
  缺失或形状不对时安全降级为空，不会把渲染带走。
- **顶部计数写的是"个文件"。** 提交图顶部的数字借用的是文件计数那条文案，而它数的是提交。

## 变更

- **抽屉默认宽度从视口的 50% 提到 80%**，并**删掉了 1600px 的像素上限**——那个上限与"默认
  80%"直接冲突：2560 的屏幕算出来 2048，却被夹回 1600（只有 62%）。保护"不能把主界面吃掉"
  的正是 80% 这个比例本身（留 20%）。有持久化宽度时仍然优先用持久化值，双击手柄/按 Home
  复位回 80%。
- **滚到底会自动加载下一页**（距底 320px 触发；同一 `ref + skip` 只有一个请求在飞，因此连续
  滚动或连点都不会重复发请求；`hasMore` 为假即停止）。底部只在加载时显示「正在加载更多…」，
  **不会整页 loading 或白屏**；「加载更多」按钮保留下来做失败重试。**任何分页都只追加中栏**：
  左栏的数据源永远只由未过滤的第一页写入，因此它不会在滚动中自己变内容。取舍：更深历史里的
  分支不再自动出现在左栏（左下角那句说明写清了这一点），彻底方案是宿主侧提供 refs 快照。
- **提交信息输入框从 2 行改到 4 行**（另加 90px 最小高度，仍可纵向拖大），因此一条正常的
  "标题 + 说明"能一次写完；提交按钮与"提交并推送"仍然固定在底部，不会掉出可视区。
- **字号跟随「设置 → UI 字号」。** 抽屉里的字号（提交详情、改动文件列表、差异正文、增删行数、
  状态徽标、按钮与标题）全部从 `--dsh-ui-px-14` 派生，基准 14px 下与原值**逐像素相同**，
  字号调到 12 / 18 时整块同步缩放。
- **新增「✨ AI 补充」：按已勾选的文件草拟提交信息。** 输入只来自你勾选的那批文件（不是整个
  工作区），宿主侧对文件数（30）、单文件字符数（3000）、总字符数（30000）三层设限，超出的
  文件降级为"状态 + 增删行数"而不是把整仓库塞进上下文。生成走宿主正式能力——`ctx.llm.stream()`
  （与 agent loop、会话标题同一条路径）加上你在设置里选定的默认模型，因此**登录与模型配置天然
  复用**；插件内没有 API Key、没有硬编码地址、没有新建 provider。宿主没装模型能力时会明确
  说明缺哪一个服务。交互上：生成期间按钮禁用并显示"生成中"；输入框为空就直接填入；**已经
  打了字绝不静默覆盖**，而是让你选「替换 / 追加 / 取消」；生成期间切项目或改勾选会让旧响应
  作废；失败保留原文，只给一条非阻塞提示。

## 校验

- 新增 `scripts/test-review-commit-message.mjs`（**60 项**断言）：三层上限（100 个文件只取
  30、单文件截断到 3000、超总量后**不再读盘**）、二进制与读不到时降级、提示词把路径当数据
  而不是指令、输出规范化（去代码围栏/前缀/引号、标题超长截断）、失败翻译，以及"宿主缺能力
  时点名缺 `ctx.llm` 还是 `ctx.agentDefaultModel`"。用假 `stream` 钉住请求形状（provider/model
  来自默认模型选择、不传封闭联合的 `purpose`、不传无归属的 `sessionId`）。
- `scripts/test-review-staging.mjs`（**160 项**）新增：未跟踪文件展开差异（不抛错、只发一次
  请求、请求带 `untracked: true` 与 HEAD、界面上真画出内容、收起再展开走缓存、切项目重新取）；
  「AI 补充」交互（只带勾选文件、生成中禁用且不重复发请求、输入框为空直填、已有输入给三选一、
  追加/替换/取消各自的结果、失败保留原文并可重试、未勾选时禁用、切项目后旧结果不写入）。
- `scripts/test-review-graph-view.mjs`（**153 项**）：计数用 `graphCommits` 且过滤后跟着变、
  中栏逐行不显示哈希且时间到秒、滚到底只发一次 `skip=4`、`hasMore=false` 后不再请求、
  **分页在飞那一帧三栏与已有提交仍在且只显示"正在加载更多…"**、分页失败保留列表并可重试、
  **字号的设计值集合与改造前逐一相同**（样式侧 `11,11.5,12.5,13,17`，内联侧含 `12.5/11.5/12`，
  没有裸 px）。
- `scripts/test-review-overlay-hooks.mjs`（**120 项**）：默认宽度 80%、**2560 屏也真的是 2048**
  （不再被像素上限夹住）、持久化优先、拖动/键盘/复位；点外关闭与三处豁免（分支菜单、dialog、
  入口）、无 `target` 的事件不抛错；输入框 4 行 + 90px 且提交区在滚动区之外、可被压缩的是文件
  列表；样式里没有裸 px 字号。
- `scripts/test-review-host.mjs`（**79 项**）新增 `/commit-message` 的路由契约：空 `files` 与
  越界路径都被挡住（400），合法入参**不会是 500**（没装模型时返回带稳定 code 的 501/502），
  GET 是 405。
- `scripts/mutation-check.mjs`（**新增**）做变异验证：把本轮的每个修复逐个改回旧写法，对应断言
  必须变红、还原后必须变绿——**12 项全部符合预期**。其中"未跟踪差异退回 `FileDiff` + `byFile`"
  会原样复现用户报的 `ReferenceError`。
- 离线全量：**26 个脚本 / 1762 项断言 0 失败**；`check-react-rules`（hook 无条件且在 early
  return 之前、不把 `ref` 当业务字段）、`check-plugin-i18n`、`check-readme`（中英）、
  `test-plugin-sync`、类型检查全部通过。
- 真实 Electron/CDP 冒烟 `scripts/test-project-git-smoke.mjs` 扩到 **A–H**：新增 F（默认宽度
  80%、点外部关闭且右上角入口仍在）、G（计数说"提交"、时间到秒、行里没有哈希、滚动真的分页）、
  H（未跟踪文件点开有差异且**没有 `is not defined` / ReferenceError**、已有一半输入时 AI 补充
  不覆盖）；`scripts/test-drawer-dismiss.mjs` 增加"点分支菜单内部不许把抽屉关掉"。这两个脚本
  需要带远程调试端口的实例（`npm start -- --remote-debugging-port=9333`），请在桌面会话里跑。

---

# 1.4.8

本次修掉 `Log` 页签（提交图）里两个很显眼的交互问题：**点一下左栏的分支，左栏就只剩那个分支附近
的 refs**；以及**点分支时整个 Log 先变成 loading 空白页再重新出现**。根因是左栏分支树与中栏提交
列表用了同一份数据、而刷新又用了"整页 loading"的语义。现在两者彻底分开，刷新改成后台进行。

## 修复

- **点分支之后左栏分支树被"过滤没了"。** 左栏以前直接由 `fresh.commits` 聚合 refs，而它同时也是
  中栏的数据；点 `develop` 会把中栏换成 `/graph { ref: 'develop' }` 的结果，左栏再从这些提交里
  聚合，于是 `master / feature/a / feature/b / origin/*` 全部消失——看起来像"分支树只显示当前
  分支"。现在状态分成两组：
  - **左栏**用 `treeCommits`（外加 `treeHasMore`），**只由"全部分支"（`ref === ''`）的响应写入**，
    按分支过滤的结果永远不会改写它；
  - **中栏**用 `commits`，`selectedRef` 非空时就是某个分支的过滤结果。

  因此点 `develop` 之后左栏仍然是 `master / develop / feature/a / feature/b / origin/master /
  origin/develop`（`develop` 高亮），再点一次同一个分支即取消过滤、中栏回到全部提交。左栏的选中
  态现在同时给出 `aria-selected` 与 `data-graph-tree-selected`（以前只有背景色差异）。
- **点分支时整个 Log 白屏闪烁。** 以前 `reload` 无条件把 `phase` 置成 `loading`，而视图在
  `phase === 'loading'` 时直接 return 整页 loading —— 于是三栏（连同左栏分支树与它自己的滚动
  位置）被整体卸载再重建。现在：
  - **手上没有可展示数据**（首次进入、切换项目）才显示整页 loading；
  - **已经有数据**时切分支/刷新只置 `refreshing`：`phase` 保持 `ready`，三栏 DOM 原地保留，
    工具栏出现「正在加载…」，中栏列表压暗表示"这一份是上一次的结果"；
  - 请求成功 → 中栏原地替换；若原来选中的提交不在新结果里才清空选中（右栏回到"选一条提交"）；
  - 请求失败 → **保留上一份可用提交**，只在中栏给一条非阻塞提示（不会把整个 Log 换成错误页）。
  - 顺带修好了"提交成功之后 Log 闪一下"：它走的也是这条刷新路径。
- **分支树不会因为切分支重新挂载。** 左栏的行 `key` 稳定、视图在切 ref 时不返回 loading 页，因此
  左栏的 DOM 实例、滚动位置与分组结构都由 React 原样保留，只有选中样式变化。

## 变更

- **每个 ref 的首屏有缓存**（模块级、键含工作区、上限 12 个，最近使用的留下）：因此
  `develop → master → develop` 这类来回切是**同一帧**就显示缓存那一份（左栏完全不动），随后在
  后台 revalidate；取消 `selectedRef` 恢复"全部"同样是立即的。缓存放在模块级而不是组件里，是因为
  `Changes ↔ Log` 切页签会让提交图整个重挂，实例内的缓存那样就没了。
- **分页与左栏彻底解耦**：过滤状态下的"加载更多"只追加中栏；未过滤时的分页才会顺带扩展左栏（那是
  特性——更深历史里的分支会随之出现）。任何一种分页都不会让左栏"越来越少/越来越多"。
- 左栏数据源仍然只覆盖"已加载的提交"，因此 tip 落在更早历史里的分支要等"加载更多"才出现（与之前
  一致，未变）。

## 校验

- 新增 `scripts/test-review-graph-branch-filter.mjs`（**61 项**断言）：初始左栏完整六项 → 点
  `develop`（左栏仍六项、唯一高亮、中栏换成 develop 的提交）→ 点 `master`（只换高亮与中栏）→
  再点 `master`（取消过滤、中栏回全部）→ **请求故意挂起期间断言 `data-graph-view` /
  `data-graph-tree` / `data-graph-pane=list` 一直在、左栏内容不变、出现 refreshing 提示、界面不是
  只有 `graphLoading`** → 放行后中栏原地替换 → 过滤分页不动左栏、未过滤分页扩展左栏 → 快速
  `develop → master → feature/a` 且**最新的先回、旧的最后回**（乱序）时最终只采用 `feature/a`、
  旧响应不覆盖 → 缓存命中同帧可见 → 全程 hook 数量恒定（等价于真实 React #310）。
- 三处变异验证（随后均已还原）：把左栏改回 `fresh.commits` → 复现"左栏只剩 `develop`"；把
  `reload` 与阶段守卫改回 v1.4.7 的写法 → 复现"三栏消失、只剩 graphLoading"；去掉缓存命中 →
  缓存断言变红。
- `scripts/test-project-git-smoke.mjs`（真实 Electron + CDP）新增 **case E**：把左栏滚动一段后点
  一个分支，断言左栏项数与内容不变、唯一高亮、三栏仍在、**左栏滚动位置保持**、无 React error；
  再点一次取消过滤后左栏仍完整。
- 22 个 Node 测试脚本合计 **1448 项**断言全部通过；`check-react-rules`、`check-plugin-i18n`、
  `check-imports`、`check-readme`、`check-version`、`test-plugin-sync`、`test-i18n`、类型检查
  全部通过。

---

# 1.4.7

本次继续收口**项目级 Git**（右上角入口 + 项目改动抽屉 + 分支徽章）：修掉三处让"入口/抽屉整个
消失"的渲染期崩溃、把入口与面板彻底隔离开，并把 `Changes` 页签的**全仓库差异**改成按文件按需取
——切项目的等待从秒级降到百毫秒级。另外修了分支操作菜单"关不掉"的状态机。

## 修复

- **切换项目后项目 Git 入口与抽屉一起消失（Rules of Hooks）。** `StagingSection` 里有一个
  `useMemo`（按文件切分差异）写在**阶段守卫之后**：加载中那一帧只调用 15 个 hook，数据到手
  变成 16 个，React 抛 `#310 Rendered more hooks than during the previous render` 并**卸载整棵
  抛错子树**——而那棵子树的根正是右上角入口组件，外层槽位的错误边界再把它整体换成错误占位。
  现在：那个 `useMemo` 随"全仓库差异"一起去掉（见下），阶段守卫整体移到**所有 hook 之后**；
  并且顺带修掉全插件另外**6 处条件调用 hook**（`typeof useSessions === 'function' ? useSessions(...) : ...`
  这种写法在"源出现/消失"时会让 hook 数量变化），统一改成首次渲染锁定的 `useLatchedHook`。
  新增 `scripts/check-react-rules.mjs` 静态拦截这一类形状，客户端回归测试里也加了"hook 数量
  必须恒定"的校验（等价于真渲染器的 #310）。
- **点 `Log` 时报 React #290。** 分支树收到的是 `ref: fresh.ref`——`ref` 是 React 的**保留键**，
  传给函数组件既不进 props（`props.ref === undefined`，于是"筛选命中的那一行高亮"一直是一段
  死代码），又触发 `Element ref was specified as a string but no owner was set`。业务字段改名为
  `selectedRef`；静态检查里加了一条"不许把 `ref` 当业务字段传给组件"的规则。
- **入口按钮与面板现在是两条互不连累的子树。** 右上角按钮（`ProjectChangesTriggerButton`）与
  面板（`ProjectGitPanelErrorBoundary` → `ReviewPanel`）拆开：面板内部（Changes / Log / 暂存区 /
  提交框）任何渲染期异常都只让**面板**显示「Git 面板加载失败 + 详细错误 + 重新加载 + 关闭」，
  入口永远在。`Log` 页签里保留更细的一层边界。
- **切项目不再临时回退到上一个项目。** 以前 `session ?? hostCurrent ?? candidates[0]` 无法区分
  "根本没有当前会话"与"已经切到新会话、但 cwd 还没加载出来"，于是会先显示上一个项目（或外壳
  目录）的数据再跳到新项目——右上角数字与列表因此看起来"对不上"。现在保留 `hasCurrentSession`：
  有会话就只认它的 cwd，cwd 未到则显示「正在切换项目…」，**不发请求、不显示任何旧数据**；
  只有确实没有当前会话时才允许兜底。
- **分支操作菜单"关不掉"。** 同一行再点一次会先被 document 的 mousedown 关掉、随后那个 200ms
  的"单击延迟弹菜单"定时器又把它冒出来。现在：同一行再点一次**立即关闭且不再重弹**；点别的一行
  旧菜单立即消失、最终只留目标那一行的菜单；点列表空白/分组标题、滚动列表都会关菜单**并取消
  待弹定时器**；双击清定时器 → 关菜单 → 最多一次 checkout（没有菜单闪现）；Esc 先只关菜单；
  点菜单项（含「签出」）**先关菜单再执行动作**。

## 变更

- **项目级快照改成元数据级。** `/workspace` 以前为了"改了 N 个文件"这个数字要建一棵临时索引树
  （`read-tree` + `add -A` + `write-tree`）**再算一份全仓库统一差异**（实测 38.7 MB）；现在只用
  `status --porcelain=v2 --branch -z --untracked-files=all`（一次拿到分支/HEAD/每个文件的索引与
  工作区状态）+ `diff --numstat HEAD`（只有行数、没有正文）。入口数字与 `Changes` 的三个分组仍然
  来自**同一份** `files`。
- **逐行差异按需取。** 新增 `POST /review/workspace-file`（`{workspace, path, revision, untracked}`）：
  点开哪个文件才算哪个文件（未跟踪文件走 `--no-index` 与空文件比），单个文件仍有大小上限与
  `truncated`，二进制照常给 binary 提示。客户端缓存键是 `workspace + HEAD + 路径`，因此切项目、
  提交、切换分支后旧差异自然失效；迟到响应有令牌与键双重把关，不会把 A 文件的差异画到 B 下面。
  `Changes` 页签里那条"依赖全仓库差异"的路径（`splitByFile(snapshot.diff)`）随之删除。
- **快照状态机补全。** 每个工作区明确 `idle / loading / ready / error / notrepo`；已有数据时刷新走
  **stale-while-revalidate**（保持 `ready` + `refreshing`，不清空列表，不再闪成空白）；失败时
  有数据就保留数据并标出 `refreshError`，没数据才进 `error`——不会永远停在"正在读取差异"；
  每次请求带 `workspace + generation + requestId`，旧工作区/旧请求的响应一律静默丢弃。
- **项目级 Git 抽屉不再是 popover。** 点左侧项目、点聊天正文、点主界面其它区域都**不会**把它
  关掉——只有 X 按钮、Escape、再点一次右上角入口才关。会话内那个审查标签仍按点外部关闭。
- **轮询路径明确"只要元数据"。** 输入框上方的改动数字每 10 秒轮询 `/changes`，现在带
  `metadataOnly: true`，不再让那条路由在后台反复计算全仓库统一差异。

## 性能（真实仓库：6,639 个改动/未跟踪条目）

| | 之前 | 现在 |
|---|---|---|
| 轮询一次的 git 进程数 | **10** | **2** |
| 轮询一次的耗时 | **6.26 s**（冷索引首轮实测 163 s） | **0.32 s** |
| 轮询产出的差异正文 | 全仓库统一差异 **38.7 MB** | **0** |
| 点开一个文件 | 复用整份差异 | **1 个进程 / 95 ms / 1 KB** |

用 `scripts/measure-workspace-snapshot.mjs` 可复现（只读，临时索引落在系统 temp）。

## 校验

- 新增 `scripts/check-react-rules.mjs`：静态拦截 React **#310**（hook 在 early return 之后 /
  条件调用）与 **#290**（把 `ref` 当业务字段传给函数组件）——这两类只有真实渲染器才报，假 React
  单测抓不到。变异验证：把 `byFile` 放回守卫之后、把 `ref` 改回业务字段，都会变红。
- 新增 `scripts/test-review-project-git.mjs`（61 项）：假 React **逐组件比较 hook 数量与顺序**，
  覆盖 A ready → B loading（入口/抽屉在、不显示 A 的文件）→ B ready → 回 A、"正在切换项目"
  且不发请求、面板级降级 + 重新加载 + 关闭、按需差异请求、快照状态机（SWR / 失败保留数据 /
  `requestId`）。
- 新增 `scripts/test-review-lazy-diff.mjs`（38 项）：不点不取、点一次只取一次、折叠不取、
  缓存命中、换 HEAD / 换 workspace 重取、迟到响应不串文件、loading 与 error 态。
- 新增 `scripts/test-project-git-smoke.mjs`：**真实 Electron + CDP 冒烟**（case A 切项目、
  case B 点 Log 不出 #290、case C Log/Changes/切项目循环 5 次、case D 快速 A→B→A），监听
  `window.onerror` / `unhandledrejection` / `console.error`，出现 React minified error 即失败；
  并校验页面加载的是修好之后的 bundle。需要一个带远程调试端口的实例：
  `npm start -- --remote-debugging-port=9333`。
- `test-gitbar-branch-interaction` 从 25 项扩到 **45 项**（新增第 5 节：菜单关闭状态机）。
- `test-review-host` 从 51 项扩到 **72 项**（新增 `/workspace` 不带差异正文、`/workspace-file`
  单文件差异、未跟踪文件、非 ASCII / 带空格路径、`/changes` 的 `metadataOnly`）。
- 21 个 Node 测试脚本合计 **1387 项**断言全部通过；`check-react-rules`、`check-plugin-i18n`、
  `check-imports`、`check-readme`、`check-version`、`test-plugin-sync`、`test-i18n`、类型检查
  全部通过。

---

# 1.4.6

本次修掉两个"看起来像别的问题"的实机故障：**点开项目改动抽屉的 `Log` 页签之后，整块抽屉连同
右上角入口一起消失**；以及 **1.4.5 里分支补算的字段契约不一致，导致补算会一轮轮重复触发**。
同时把提交图的数据入口、错误降级与几处异步交错补齐。

## 修复

- **点 `Log` 之后抽屉与右上角入口一起消失。** 这个现象的因果很容易判错：点 `Log` 只做了一次
  `setTab('log')`，所以它看起来像"面板被关掉了"。实际是**渲染期 TypeError**——提交图在渲染时读
  host 回来的字段，字段一旦缺失（例如 `commit.parents` 是 `undefined`）就抛
  `Cannot read properties of undefined (reading 'length')`；React 在没有错误边界时会把**抛错的
  那棵子树整个卸载**，而那棵子树的根正是右上角入口组件，外层槽位的错误边界再把它整体换成错误
  占位。于是"抽屉 + 入口"同时没了。现在：
  - `/graph` 与 `/commit-detail` 的响应在**取数处统一规范化**（`parents`/`refs` 一律数组、
    文本字段一律字符串、没有 `hash` 的条目直接丢弃），host 的畸形数据**不可能**进入渲染层；
    坏数据只会显示得难看，界面照常出图。
  - 渲染层里每一处"看起来天经地义"的访问（`commit.parents.length`、`refs.slice(0,3).map(...)`、
    `committedAt.slice(0,10)`、`containing.join(...)`）都改为先收敛形状；差异解析对非字符串输入
    也不再抛。
  - `Log` 页签内容外面加了一层**局部错误边界**：图炸了只会让这一页显示「重新加载 Log」与可诊断
    的错误详情（异常原文、组件栈、工作区），抽屉、`Changes` 页签与右上角入口**都不会被带走**。
    异常同时进 console 与 `window.__dshDesktopReviewLogError`——不再有任何"静默降级"。
- **提交图的字段契约不一致（1.4.5 引入）。** `/branch/sync` 回的是 `exact`，而客户端条目上读的是
  `syncExact`，两者并存时"这个分支已经精确过了"永远判不出来，补算请求被反复重发，最终把整个
  仓库的分支都算一遍。现在**同一个概念只有一个字段名**（`syncExact`），并且补算严格有界。
- **写操作之后的分支列表刷新。** write 抢占 `branches` 分片之后，旧的 single-flight 条目已经作废；
  此时必须**真的发出新请求**，否则新列表既不会来、加载态也不会结束（界面停在旧数据上）。
- **切换提交后文件差异"串提交"。** `Log` 里两次提交改动同一个文件是常态，而文件行的 React key
  只有路径，切提交时会被当成同一个实例、沿用上一条提交已取回的差异（路径一样，界面上几乎看不
  出来）。key 改为 `revision:path`，并给迟到的差异响应加了令牌。
- **提交成功后刷新历史拿不到新提交。** 图刷新（`refreshToken` 变化）以前会被合并进**提交之前**
  那个还在飞的 `/graph` 请求里，而提交图不轮询——界面会一直停在旧历史上。现在刷新是另一个请求，
  并抢占 `graph` 分片，旧响应回来时被丢弃。

## 有界性（分支补算）

- 精确 ahead/behind 只对**屏幕上真正可见的行** + 用户**选中的分支**补算，不再"第一批 32 个完成后
  自动接着算后面的"。可见性用视口交叉判定（拿不到 DOM 时退化为固定 12 行窗口）。
- 自动补算有与仓库规模无关的常数上限；已精确、无上游、当前分支、已请求过的一律跳过。

## 校验

- 新增 `scripts/test-review-log-tab-crash.mjs`（**78 项**断言）：用带错误边界与槽位级隔离语义的
  桩渲染器，逐步钉住 0 规范化纯函数 / A 正常数据点 `Log`（图、入口、页签栏都在）/ B host 少给或
  给错字段（`parents` 缺失、`refs` 是字符串、`committedAt` 是数字、`hash` 缺失、详情 `files`
  不是数组）不崩且照常渲染 / C 图子树抛错后只降级这一页、诊断信息齐全、重试能恢复 / D `Log`↔
  `Changes` 切换仍工作且改动数量不变 / E 提交后刷新必发第二个请求、旧响应不许覆盖新页。
- 新增 `scripts/test-gitbar-branch-sync.mjs`（36 项）与更新 `test-gitbar-branch-perf.mjs`（22 项）：
  `syncExact` 契约、补算有界性（不许自动连续补算、累计上限、参数化 single-flight）。
- `test-gitbar-workspace-race`（29 项）新增"write 抢占在途 branches 请求"一节；
  `test-review-workspace-race`（52 项）、`test-review-graph-view`（61 项）覆盖跨提交差异隔离。
- 19 个 Node 测试脚本合计 **1249 项**断言全部通过；`check-plugin-i18n`、`check-imports`、
  `check-readme`、`check-version`、`test-plugin-sync`、`test-i18n`、类型检查全部通过。

---

# 1.4.5

本次对**项目级 Git**（输入框上方的分支徽章 + 右上角的项目改动抽屉）做了一次系统性重构：
修掉切换项目时的串数据、把分支列表从"按分支数起进程"改成一次枚举、把抽屉改成 IDEA 的
`Changes | Log` 两个页签。顺带修掉了发布说明本身的一个事故。

## 修复

- **切换项目 / 切换对话后不再串数据。** 此前每个异步请求都是"发出去 → 回来就写状态"，
  没有"这次响应属于哪个工作区"的判定，于是旧项目的响应晚回来就会盖掉新项目的数据
  （表现时有时无，取决于两个请求谁先回来）。现在 gitbar 与 review 共用同一套**工作区世代
  闸门**：请求带上工作区 + 代际 + 序号，回来时只有仍属于当前工作区、且仍是该状态分片最新
  的一次才允许落地；旧工作区的响应**静默丢弃**，连加载态与忙碌态都不会被它关掉。
  切换工作区的**那一帧**就进入加载态，不再显示上一个项目的分支或文件。

- **"外面显示 0，进去却有文件"。** 入口按钮的数字与抽屉里的文件列表以前来自**两条独立轮询**
  （入口每 10 秒打一次 `/workspace`，抽屉里的"变更"区块走 `/status`），两份缓存对不上是必然
  的。现在工作区级的 Git 数据只有**一处**：共享快照 store。入口数字就是
  `snapshot.files.length`，抽屉里列出的就是同一份 `files`，三个分组的数量也由同一份数据
  现场过滤得出——分组数量与列表恒等，不可能再对不上。

- **分支列表加载慢。** 此前是"一次 `for-each-ref` + **对每个有上游的分支各跑一次
  `rev-list`**"，而且截断发生在这轮之后：300 个分支就是 300 个 `git.exe` 同时启动。
  现在首屏只起 **1 个**进程，精确的领先/落后只对**当前可见**的分支异步补算，并发上限 4。

- **写操作后的等待。** `checkout / merge / rebase / fetch` 等不再同步等待完整的分支枚举：
  响应立刻回最新状态 + `branchesStale`，分支列表由客户端异步刷新。

- **当前分支的领先/落后恒准。** 改为直接取 `git status` 的 `branch.ab`（git 自己算的），
  不再受"本地跟踪引用过期"影响。

## 变更

- **分支行改为 IDEA 的交互**：**单击**选中该行并打开它的操作菜单（约 200ms 的延迟用来区分
  双击，因此正常双击**不会**先闪一下菜单），**双击**才切换分支，**右键**打开的是同一个菜单。
  当前分支不再整行禁用——单击照样能开菜单（新建分支等），只是"签出自己 / 合并到自己 /
  变基到自己 / 删除自己"这些动作保持禁用。
- **项目改动抽屉改成 `Changes | Log` 两个页签**（IDEA 的 Git 工具窗同款），不再是一列叠到底
  的"暂存区 → 更改 → 文件列表 → 最近提交"：
  - `Changes`：**已暂存 / 更改 / 未跟踪** 三组，点文件展开逐行差异，每行可暂存 / 取消暂存 /
    还原（还原会先弹确认）/ 查看该文件的变更记录；提交信息与「提交」「提交并推送」**固定在
    底部**，不再随超长文件列表滚走。
  - `Log`：**分支树 / 提交图 / 详情** 三栏。单击一条提交只在右侧显示它的详情与改动文件
    （**不再在原位置展开**），点文件看这次提交对该文件的差异；两条分栏可拖动调整宽度并
    **记住**，窄窗口可以把分支树或详情收起来；顶部有刷新与搜索。
- 远端列表改由单独的路由给出（一次 `git config` 拿全部，而不是"1 + 远端数"个进程）。

## 性能（同一台机器、同一个 **300 分支**仓库实测）

| 指标 | 之前 | 现在 |
|---|---|---|
| `GET /branches` 起的 git 进程 | **304** | **1** |
| `GET /branches` 耗时 | **5156 ms** | **509 ms** |
| 精确 ahead/behind（32 个可见分支） | 每分支一个进程、不限并发 | 33 个进程，**最高并发 4** |
| 当前分支的领先/落后 | 与其他分支同样的 rev-list | 来自 `git status`，恒精确 |

## 发布说明本身的修复

- **Release 正文不再带着整份变更日志。** 发布工作流此前是 `cat RELEASE_NOTES.md` 把**整个
  文件**塞进正文，而它是累积的变更日志——于是 v1.4.4 的正文里有 14 个版本标题（一直回到
  1.3.1，共 2.4 万字符）。现在由 `scripts/release-notes.mjs` 取**本次那一节**（CI、
  `release.mjs` 的发布前校验、回归测试共用同一份实现），历史上受影响的 v1.3.2–v1.4.4
  **共 12 个** Release 正文已裁回本次版本（只改正文，标签与附件未动）。

## 校验

- 新增四个回归测试脚本（合计 **117 项**断言）：
  * `test-gitbar-workspace-race`（19 项）：A 的请求故意延迟 → 切到 B → B 先回 → A 最后回，
    界面必须仍是 B；A→B→A 快速切换、切换当帧的加载态、轮询不重叠、写操作收尾不许漏进新工作区。
  * `test-review-workspace-race`（52 项）：入口数字 == 头栏计数 == 页签里的行数 == 快照
    `files.length`；新增/删除/未跟踪后两处同步变化；同一工作区的过期响应不许覆盖新数据；
    提交图的 `reload` 抢占在途的 `loadMore`；分栏拖动持久化、搜索、收起。
  * `test-gitbar-branch-interaction`（25 项）：单击不 checkout 且延迟开菜单、`click→click→dblclick`
    只发一次 checkout 且全程不弹菜单、当前分支单击开菜单而双击无动作、右键与单击菜单条目一致。
  * `test-gitbar-branch-perf`（21 项）：300 分支下 `/branches` 的进程数上界、补算的并发上限、
    名字数封顶，并给出新旧的进程数对照（量法：`GIT_TRACE2_EVENT` 按进程计数）。
- 既有测试同步到新交互与新数据结构：`test-review-staging` 114、`test-review-overlay-hooks` 89、
  `test-gitbar-branches` 159、`test-gitbar-source-panel` 98、`test-review-drawer-style` 59 等。
- 本次共 **1114 项**断言全绿（含 `test-graph-layout` 184 + parity 19、`test-review-graph` 138、
  `test-review-host` 51、`test-plugin-sync`、`test-release-notes` 19、`test-i18n`、
  `check-plugin-i18n`、`check-imports`、`check-readme`、`check-version`、`test-version`）。

---

# 1.4.4

本次重做「项目改动」抽屉的**观感与布局**：更现代、更好操作。

## 变更

- **外观集中到一层样式，不再各处内联。** 此前同一类控件（行、图标按钮、徽标、分区标题）各写一份内联样式，行高 26/20/18 混杂、圆角 4/6/7 混杂，一屏里能看出七八种不同的节奏；而 `:hover` / `:focus-visible` 这类规则**内联根本写不出来**，于是"这一行能不能点、这个按钮是什么"只能靠猜。现在统一到一组类与设计令牌上：控件一律 24–26px 高、行最小高度 28px、圆角 6/8/10 三档、间距走 4 的倍数。

- **头栏回答"我在哪个分支上提交"。** 标题旁边加了当前分支徽标（数据取自**已有的**历史响应，不额外打一次 git），右侧是计数、刷新与收起。

- **提交区做成一张卡片。** 提交信息框有完整的焦点环与占位符样式，"提交"是唯一的实心主按钮、"提交并推送"是描边次按钮，两者高度一致；提示行里补上了 `Ctrl+↵` 快捷键——它本来就实现了，但不写在界面上没人会去试。

- **行内动作改为悬停浮现。** 每行末尾的「变更记录」由四个字改成图标（文件名不再被挤到省略号里），它与 `+/−` 一起默认收敛、鼠标悬停或键盘聚焦时才浮现。两个例外：**已暂存**那一行的动作始终可见（它代表"这个文件已在索引里"，藏起来就找不到取消暂存的入口），以及触摸屏设备上全部常显（那里没有悬停）。

- **行与分区有了层次。** 已跟踪改动的行、未跟踪文件的行统一了缩进、间距与悬停底色；分区标题用小号加粗 + 次级色表达层级（不再用大写转换——它对中文没有效果，却会让混排的英文单词像另一种字体）；「未跟踪」组的"加入 git"与已选数量收进一条贴底的汇总栏，文件多的时候不必滚到底才知道能做什么；"没有未提交改动"变成一个居中带对勾的安静空状态。

## 校验

- 新增 `test-review-drawer-style`（**58 项**）。它跑在**自己的进程**里：抽屉的 hook 槽按树中位置归属，而 `test-review-overlay-hooks` 前七节已经把若干行的展开状态写了进去，"换一个挂载 key"并不等于新挂载（嵌套组件不参与 key），于是新断言会全部误红。这一节断言的是**结构与样式钩子**——头栏/标题容器/分支徽标、提交卡片与主次按钮钩子、分组头与计数徽标、行内动作的收敛钩子、未跟踪汇总栏，以及样式块确实注入、收敛规则同时覆盖悬停/键盘聚焦/触摸屏、样式块括号平衡。
- `test-review-overlay-hooks` 补上头栏与分支徽标的断言（分支名确实渲染成 `main`），并把 `drain` 修成"反复展开并执行**嵌套组件**的副作用直到队列为空"——此前它丢掉了嵌套组件产生的 effect，于是新挂载的子树永远停在加载中。
- 其余全部通过：`test-review-graph` 138、`test-gitbar-branches` 153、`test-gitbar-source-panel` 97、`test-review-staging` 102、`test-review-graph-view` 51、`test-graph-layout` 184 + parity 19、`test-review-host` 51、`test-i18n`、`check-plugin-i18n`、`test-plugin-sync`、`test-version`。

## 说明

- 所有 `data-*` 标记、`title`、`role`、`aria-*` 一律未改：脚本与无障碍读的都是它们，改它们等于改接口。
- 光靠桩渲染看不到颜色与间距，所以这次同时确认了"结构钩子齐全"，真实观感需要以实际界面为准。

---

# 1.4.3

本次是 1.4.2 的一处收尾：**详情还没到手时，面板外壳就已经在了**。

## 变更

- **「这条提交被展开了」有了一个稳定的锚点。** 改动面板的 `data-review-commit-changes` 此前只在详情返回**之后**才渲染；加载中是另一个结构，于是"点了没反应"和"正在加载"在界面上、在脚本里都分辨不出来。现在外壳始终渲染（带 `data-review-commit-changes` 与它所对应的提交哈希），加载中/出错态显示在外壳**里面**。

## 校验

- `test-review-overlay-hooks` 从 80 项扩到 **86 项**，新增第 8 节：把 `commit-detail` 的响应挂起，断言加载中就已经有面板外壳、外壳带着正确的提交哈希、还没有文件行；放行后文件行出现，且**没有重复请求**。
- 其余全部通过：`test-review-graph` 138、`test-gitbar-branches` 153、`test-gitbar-source-panel` 97、`test-review-staging` 102、`test-review-graph-view` 51、`test-graph-layout` 184 + parity 19、`test-review-host` 51、`test-i18n`、`check-plugin-i18n`、`test-plugin-sync`、`test-version`。合计 **881 项断言**。

---

# 1.4.2

本次把「最近提交」做成**可以点开的**：点提交记录看它改了哪些文件，再点文件看具体改了什么。

## 变更

- **点提交记录 → 看到这次提交改动的文件。** 此前「最近提交」只是一列只读的标题，想看某次提交动了什么必须另开终端。现在每一行都可以点开：展开后显示这次提交的**元信息**（标题、短哈希、作者与邮箱、时间、所在分支、提交正文）与**改动的文件列表**（状态徽标 + 增删行数），点标题行上的箭头可以收起。

- **点文件 → 看到这个文件在这次提交里的差异。** 差异是**按需取**的：打开提交只拉文件清单，点某个文件才去取它的 `git diff`（用 `git show`，根提交用 `--root` 而不是拿工作区去比）。一次提交改几百个文件时不会因为"顺手把全部差异都拉回来"而卡住。

- **两处显示同一份信息。** 抽屉里的「最近提交」与主区域的提交图共用同一对组件（`CommitSummary` / `CommitFileList`），因此元信息、文件列表、点开行为完全一致——各写一遍必然漂移。

## 修复

- **「在 N 个分支中」在提交图里不显示。** 这一行读的是 `props.commit.containingBranches`，而详情接口把分支集合放在**结果对象的顶层**而不是 `commit` 里，于是永远取到空数组、整行不渲染。现在由调用方把已经拿到的分支集合直接传进来（只给哈希的自取路径仍从取回的详情里读）。
- **收起再展开会重复请求一次提交详情。** 详情里新加的 `containingBranches` 是每次渲染新建的数组字面量，写进 `useEffect` 依赖数组会让 effect 每次都重跑。改为把它排除在依赖之外（调用方给了 `commit` 时本就不需要取）。

## 校验

- `test-review-overlay-hooks` 从 **40 项扩到 80 项**，新增的 40 项把"点提交 → 看文件 → 点文件 → 看差异"整条链路逐步钉住：
  * 不点就不取数据（未展开时 `commit-detail` 请求数为 0）；
  * 点开后出现改动面板、面板对应被点的那次提交、`aria-expanded` 置位；
  * 详情**只取一次**，且请求带上正确的工作区与哈希；
  * 文件清单来自这次提交、元信息（标题/短哈希/作者/邮箱）真的渲染出来；
  * 所在分支那一行按"确有分支信息才渲染"断言，并校验文案函数收到正确的分支集合与数量；
  * 展开提交时**仍不取**任何文件差异；
  * 点文件才发出 `commit-file` 请求（带路径与哈希），差异行渲染出来且含新增行；
  * 再次点击收起差异 / 收起改动面板，且**都不重复请求**。
- 顺带修掉桩本身的两个坑（不改产品代码，但此前会漏报）：`collectHostNodes` 丢掉嵌套组件的副作用，于是"点了提交什么都不发生"；以及 `t` 只回显 key、分不清"没渲染"与"渲染了但没插值"。
- 其余全部通过：`test-review-graph` 138、`test-gitbar-branches` 153、`test-gitbar-source-panel` 97、`test-review-staging` 102、`test-review-graph-view` 51、`test-graph-layout` 184 + parity 19、`test-review-host` 51、`test-i18n`、`check-plugin-i18n`、`test-plugin-sync`、`test-version`。合计 **875 项断言**。

## 说明

- 主区域的提交图（三栏视图）本来就支持"点提交看文件、点文件看差异"，本次让抽屉里的「最近提交」具备同样的能力，并把两处的渲染收成一份实现。
- 差异过长或归档过大时仍按 1.4.0 的方式降级显示提示，不会把界面撑死。

---

# 1.4.1

本次把提交做成**一步到位**：改完直接提交，不再需要先「暂存」。

## 变更

- **改了就能直接提交。** 这一版之前，"更改"与"已暂存"是两个列表、勾选框默认全不勾，于是提交前必须先暂存一次——而 IDEA 里"变更"本来就等同于待提交，git 的索引是它内部处理的细节（实际反馈："我 idea 中变更提交就直接提交到本地 git 了，但是这里还要先加暂存"）。

  现在：**已跟踪改动合成一个「更改」列表，默认全部勾选**，填上提交信息点「提交」即可 —— host 会先把这批路径 `git add` 再 `git commit`，未暂存的文件一样能提交。勾选框退化为**排除**个别文件的工具（取消勾选就不提交它），而不是"先勾才有东西可提交"。

- **同一个文件不再出现两行。** 此前按索引态拆成"已暂存 / 更改"两组，`MM` 的文件（索引与工作区都有改动）会同时出现在两个列表里，用户还得判断该提交哪一个。现在按路径去重、只显示一行；索引里已有改动这件事由该行的动作按钮体现（`−` 取消暂存 / `+` 暂存）。

- 「提交并推送」「每个文件看变更记录」「未跟踪文件勾选后加入 git」都保持不变。

## 校验

- `test-review-staging` 从 100 项扩到 **102 项**，其中针对本次变更的核心断言：
  * 已跟踪改动**只有一组**，`MM` 的文件**只出现一次**；
  * **默认全部勾选**（这是"不用先暂存"的直接依据）；
  * 只填提交信息就能提交，且默认提交**全部**已跟踪改动；
  * 取消勾选一个后，只提交剩下的那些；
  * 全部取消勾选时提交按钮禁用并提示先勾选（那是明确的意图）；
  * 索引里已有改动的行显示"取消暂存"、纯工作区改动的行显示"暂存"。
- 其余全部通过：`test-review-graph` 138、`test-gitbar-branches` 153、`test-gitbar-source-panel` 97、`test-review-graph-view` 51、`test-graph-layout` 184 + parity 19、`test-review-overlay-hooks` 40、`test-review-host` 51、`test-i18n`、`check-plugin-i18n`、`check-imports`、`test-plugin-sync`。合计 **835 项断言**。

## 说明

- 想保留"先暂存、再分几次提交"的习惯也可以：「更改」组标题右侧仍有「全部暂存」，每行的 `+` 仍然可用。
- 未跟踪文件**不**默认算在提交里——把 `git add .` 的语义强加给一次普通提交，会把构建产物之类一起提交进去。要带上它们就勾一下（在「未进行版本管理的文件」组里）。

---

# 1.4.0

本次把「项目改动」抽屉做成 IDEA Git 工具窗那样的提交流程：**能勾选要提交哪些文件**、**提交 / 提交并推送**、**每个文件都能看变更记录**，并且「更改」与「未进行版本管理的文件」两组都能全选与取消全选。

## 新增

- **勾选要提交的文件。** 「已暂存」与「更改」两组的每行都有勾选框，组标题上有全选（部分选中时显示为半选）。勾好之后提交按钮变成「提交选中 N 个」，**只提交勾中的那些**。

  未暂存的文件也能直接被提交：host 收到这批路径会先 `git add` 再 `git commit`，其余一律不碰。这正是 IDEA 的行为——它也是提交那一刻才真正 add。想先暂存再挑着提交也照旧可用（勾选留空时提交索引里现有的全部内容）。

- **提交并推送**（对应 IDEA 的 Commit and Push）。提交成功后继续 `git push` 到当前分支的上游（用仓库自己的上游配置，不指定远端——那会把"该推到哪"从你的 git 配置里抢过来）。

  **推送失败不算提交失败**：提交已经落到本地历史里了，界面明确说「已提交，但推送失败：…」。把两者混成一个错误会让人以为什么都没发生，于是再提交一次。

- **每个文件都能看变更记录。** 每行右侧的「变更记录」列出该文件历次提交（短哈希 + 标题 + 日期，悬停给出完整哈希与作者）。用 `git log --follow`，因此**重命名之后仍然能追到改名前的提交**——那正是最想看的"这个文件原来是什么"。未跟踪的文件也能点，历史为空时显示「还没有提交记录」，不是错误。

- **两组都能全选 / 取消全选。** 「更改」与「未进行版本管理的文件」的组标题各有一个勾选框，同时显示当前状态（部分选中为半选）。

## 校验

- `test-review-graph` 从 114 项扩到 **138 项**，新增（都在一次性临时仓库上跑真实 git）：
  * 只提交勾选的文件——**未选中的仍是未跟踪、也没有进这次提交**；
  * 越界路径（`../outside.txt`）挡在 `git add` 之前，回 400 `unsafePath`；
  * 提交并推送——用本地 bare 仓库当远端（全程离线），断言本地 HEAD 前进且**远端 ref 与本地一致**；
  * 推送失败（把远端改成不存在的路径）仍回 200、`pushed: false`、带 `pushError`，且**提交确实落在本地历史里**；
  * `file-history`——最新一条是提交该文件的那次、每条带哈希/作者/日期、未跟踪文件回空列表、路径穿越被拒。
- `test-review-staging` 从 74 项扩到 **100 项**：两组各有全选勾选框、默认不勾、勾选后按钮文案带数量、未填信息时仍禁用且提示原因、填了信息后只提交勾中的路径、`push: true` 随「提交并推送」发出、组级全选/取消全选、每行都有变更记录按钮且点开只请求被点的那一个文件。
- 其余全部通过：`test-gitbar-branches` 153、`test-gitbar-source-panel` 97、`test-review-graph-view` 51、`test-graph-layout` 184 + parity 19、`test-review-overlay-hooks` 40、`test-review-host` 51、`test-i18n`、`check-plugin-i18n`、`check-imports`、`test-plugin-sync`。合计 **833 项断言**。

## 说明

- 「加入 git」就是 `git add`（按你确认的语义）：文件随即进入「已暂存」组。想让它只在提交时进索引，就不勾它、直接在「提交选中」里勾——那条路径同样会 add，只是时机在提交那一刻。
- 提交成功后输入框会清空（避免重复提交同一句话），这是有意为之；下一次提交需要重新填信息。

---

# 1.3.9

本次给「项目改动」抽屉加上**把未跟踪文件加入 git** 的能力（参考 IDEA 的 Git 工具窗），并把抽屉宽度改成按屏幕比例：**默认占视口 50%，最宽 80%**。

## 新增

- **未跟踪文件可以勾选后「加入 git」。** 每条未跟踪文件前面有勾选框，分组标题上有全选/全不选，勾好之后点「加入 git」就对选中的那些执行 `git add`——它们随即进入「已暂存」，与 IDEA 里 Add to VCS 的行为一致。

  * 默认**不勾选**：IDEA 里新文件也不会自动进暂存区，加哪些由用户明确决定。
  * 单行的 `+` 保留，只想加一个时不必先勾。
  * 未跟踪组因此**默认展开**（此前默认折叠）。做成可勾选的功能却默认折叠，等于让人先猜到要去哪里展开。
  * 状态响应里给出**完整路径清单**（最多 500 条，超过时说明总数与已列条数），不再是"只给 20 条样本"——要勾选就必须知道有哪些文件。

- **抽屉宽度按视口比例。** 默认 = 视口 50%，上限 = 视口 80%（另设 1600px 的像素上限，避免超宽屏上抽屉宽到失去"侧栏"的意义）。

  此前默认写死 480px、上限 72%：同一个值在 1366 的笔记本上占 35%、在 2560 的显示器上只占 19%，同一块面板在两种屏上是完全不同的东西。50% 与 IDEA 的 Git 工具窗默认占半屏一致。拖动、双击复位、`←`/`→` 微调、`Home` 复位都仍然可用，用户拖出来的宽度照旧持久化。

## 校验

- `test-review-staging` 从 60 项扩到 **74 项**：勾选框逐条存在且默认不勾、「加入 git」在无勾选时禁用、勾两个只 add 那两个、加入后清空勾选、全选/全不选、单行 `+` 只加一个。
- `test-review-graph` 从 112 项扩到 **114 项**：状态响应给出完整未跟踪清单（长度等于计数、未截断）。
- `test-review-overlay-hooks` 从 39 项扩到 **40 项**，并把宽度断言改成比例：默认 = 视口 50%（1400 → 700），向左猛拖后**停在 80%**（1400 → 1120）。
- 其余全部通过：`test-gitbar-branches` 153、`test-gitbar-source-panel` 97、`test-review-graph-view` 51、`test-graph-layout` 184、`test-graph-layout-parity` 19、`test-review-host` 51、`test-i18n`、`check-plugin-i18n`、`check-imports`、`test-plugin-sync`。合计 **783 项断言**。

## 提示

- 未跟踪文件多到几百个时（例如日志目录没进 `.gitignore`），列表会长。把它加进 `.gitignore` 仍然是最好的做法——列表上限只保证界面不被它拖垮。

---

# 1.3.8

本次修掉三处"界面上的数字与列表对不上、有时干脆读不出来"的问题，并补上总变动行数与暂存标记。

## 修复

- **大仓库下项目级 git 完全读不出来（HTTP 500）。** 实测 `E:\workspace\mmsm-amis`：该仓库有个 `tmp/` 目录，6,635 个日志文件、37.7 MB，**没有被 `.gitignore` 覆盖**。插件用 `git add -A` 给工作区拍快照，于是 `git diff --unified=3` 要输出 **45.9 MB / 1,062,664 行**，把 `execFile` 的 32 MB `maxBuffer` 撑爆 —— `/review/workspace` 直接 500。界面上的表现极具误导性：**「改动」区块显示 0 个文件／"这个项目当前没有未提交的改动"，而「最近提交」照常列出 20 条**（它走的是另一条路由），于是看起来像"项目级 git 取不到数据"。

  现在：缓冲按改动文件数估算（256 MB 上限、64 MB 下限，只用在体量与文件数成正比的统一差异上）；万一仍然超出，**不再让整条路由失败**，而是文件列表照常返回、差异留空并带 `diffOversized`，界面明确提示原因并建议把那个目录加进 `.gitignore`。单文件提交差异同样放宽。

- **分组与文件列表来自两条路由，可能对不上。** 「已暂存 / 更改 / 未进行版本管理的文件」三个分组此前读 `/review/status`，而下面的文件列表读 `/review/workspace` —— 两次独立请求、各自缓存，只要工作区在途中被切换、或某一边先返回，就会出现"分组里是 A 项目的文件、列表里是 B 项目的文件"。现在**索引态随文件一起在同一次请求里返回**（`staged` / `unstaged` / `untracked`），分组与列表由同一份数据算出，结构上不可能不一致。

- **切换对话 / 工作区后有时永久停在"正在读取差异…"。** 会话标签那条链路（`useChanges`）在"工作区还没解析出来"时只是 `return`，状态永远停在初值 `loading`。现在它进入明确的错误态并说明「当前没有可用的工作区」——与项目级那条链路此前修过的是同一个坑，只是漏改了这一个。

- **切换工作区时短暂（有时长期）显示上一个项目的文件。** 暂存区块现在核对异步结果属于哪一次请求，过期响应直接丢弃；工作区一变就立刻回到加载态，而不是继续显示旧项目的列表。

- **外部数字显示对不上。** 每个文件的行内增删数字来自 `/workspace`，而头部的计数来自文件列表长度；两处若各自计算就会漂移。现在头部同时给出**总变动行数** `+N −M`，与各行求和同源，不可能不一致。

## 新增

- **「改动」区块头部显示总变动行数**（`+N −M`，等宽数字），与每个文件的行内数字同源。
- **文件列表每行显示暂存状态**：`●` 已暂存 / `○` 未暂存 / `?` 未跟踪（悬停给出完整说明）。这是"选择性提交"能不能用的前提——在此之前用户在下面勾了暂存，上面那份列表看不出任何区别，只能靠记忆。行标记 `data-review-row` 现在带**文件路径**（此前是空标记），使"哪个文件已暂存"在 DOM 上可核验。

## 校验

- `test-review-graph` 从 103 项扩到 **112 项**：新增 `/workspace` 兼报 `staged`/`unstaged`/`untracked`，以及**暂存后同一文件变成 `staged: true`、取消暂存后变回 `false`**（用真实的 `git add` / `git restore --staged`）。
- `test-review-staging` 从 50 项扩到 **60 项**：新增文件列表的**总数等于各行之和**、三行各自的暂存标记（yes/no/untracked），以及行标记带路径。
- 新增 `scripts/probe-workspace-bigdiff.mjs`：对真实仓库（默认 `E:\workspace\mmsm-amis`）起一个临时服务端，断言 `/workspace` 返回 200、文件列表非空且标记为截断，并顺带确认 `status`/`history`/`graph` 仍正常。实测 **6,639 个文件、6.7 秒、HTTP 200**（修复前是 101 秒后 500）。
- 其余全部通过：`test-review-graph-view` 51、`test-review-overlay-hooks` 39、`test-graph-layout` 184、`test-graph-layout-parity` 19、`test-i18n`、`check-plugin-i18n`、`check-imports`、`test-plugin-sync`。

## 注意

- 仓库里**未被 `.gitignore` 覆盖的大目录**（如日志目录）会让改动列表被上千个文件淹没。把它加进 `.gitignore` 才是根本解法；本次的截断只保证界面不再因此整体失效。
- 从安装版应用里替换插件文件是**临时**的：应用每次启动都会用随包插件覆盖 `runtime/current/node_modules`（实测确认过）。要长期生效必须走一次发版。

---

# 1.3.7

本次把 git 能力从"一个分支徽章 + 一个改动抽屉"扩到三块：**源代码管理面板**（分支管理、远端操作）、**主区域的提交图**（泳道 + 提交详情），以及对应的宿主路由与测试。三块都按 VS Code 的源代码管理与 Git Graph 扩展的形态做，但每一条操作都落在真实的 git 命令上，并且都有契约测试。

## 新增

- **源代码管理面板**（输入框上方徽章点开，替换原来的"切换分支"列表）：

  * 顶部是搜索框（`搜索分支和操作`）与四个快捷操作：`更新项目…`（fetch + pull）、`提交…`、`推送…`、`新建分支…`、`签出标记或修订…`。
  * 分支按 `最近` / `本地` / `远程` 三段分组。每行从左到右是：当前分支标记、名字、同步标记（`↓N` 落后 / `↑N` 领先 / `↕N M` 已分叉 / 上游已删）、上游分支名（只留分支名，且**保留尾部**——`develop-v7.0.0-yuheng` 与 `…-kexing` 的区别在末尾）、展开箭头。
  * 分支行**右键菜单**：签出、从该分支新建、合并到当前分支、把当前分支变基到它、推送、重命名、删除。菜单项**按上下文禁用**而不是隐藏（当前分支不能签出/合并/删除自己；远程分支不能重命名——本地改名只会换掉跟踪引用的名字，远端纹丝不动）。
  * 七个对话框：新建分支（名字/起点/创建后切换）、重命名、删除确认、合并（可选 `--no-ff`）、变基、签出标记或修订、推送（远端选择 + 无上游时建跟踪）。
  * **删除未并入的分支要两次确认**：第一次不带 `force`，host 用 `git merge-base --is-ancestor` 判定未并入就回 409 `notMerged`，界面上才把"这些提交将无法从界面找回"讲清楚；点过「仍然删除」之后按钮文案变成「仍然删除」，第二次才带 `force`。
  * 合并/变基/摘取**进行中**时面板顶部显示一条提示与「中止」入口，不让用户卡在自己不认识的状态里。

- **主区域的提交图**（侧栏图标打开，占据中间主区域）：

  * 三栏：左侧分支树（`HEAD（当前分支）` / `本地` / `远程` / `标签`，点一行即按该分支筛选）、中间提交列表（多泳道彩色连线 + 提交信息 + 分支/标签徽标）、右侧提交详情（作者/时间/标题/正文、"在 N 个分支中"、改动文件列表，点文件才去取它的差异）。
  * 泳道布局是一个纯数据变换（`lib/graph-layout.js`）：按 `git log --topo-order` 的顺序算出每行的列号与连线。列表做了窗口化渲染，几万条提交的仓库也不会一次渲染几千行。

- **宿主路由**（`dsh-client-ui-gitbar`）：

  * `GET /status`、`GET /branches`（含每条分支的上游、领先/落后、最近提交时间与标题）、
  * `POST /checkout`、`/branch/create`、`/branch/rename`、`/branch/delete`、`/branch/merge`、`/branch/rebase`、`/cherry-pick`、`/remote`（fetch/pull/push）、`/op/abort`。

- **宿主路由**（`dsh-client-ui-review`）：`GET /graph`（分页，带父提交与 refs）、`/commit-detail`、`/commit-file`、`/status`（已跟踪 / 未跟踪分开）、`/untracked`，以及 `POST /stage`、`/unstage`、`/commit`。

- **更改区块的暂存与提交界面**（项目改动抽屉里）：三组文件（`已暂存` / `更改` / `未进行版本管理的文件`）+ 提交信息框。

  * 分组依据是 `git status --porcelain` 的 **XY 两列**（索引态与工作区态），**不是**差异内容——`/workspace` 那条路由给的是"HEAD vs 工作区"的内容差异，它根本不含索引态。因此同一个文件在两边都有改动（`MM`）时会**同时出现在两组里**，这是对的。
  * 每组一个有 `+`/`−` 的批量按钮（全部暂存 / 全部取消暂存）。
  * 未跟踪文件**默认折叠**，并按总数显示（实测一个真实仓库有 6,636 个未跟踪文件，一次列全会让面板变成一堵墙）；展开只列前 20 条并说明还有多少。该组的批量按钮**只对列出的样本生效**——一次 `add` 上万个文件几乎不是用户想要的，而且会让 git 跑很久。
  * 提交框：信息为空或没有暂存内容时按钮禁用，**并在旁边说明为什么禁用**（灰着不给理由，用户只会反复点）。`Ctrl+Enter` 提交；提交失败时**保留**已填信息（失败原因往往与提交信息无关）。提交成功后同时刷新改动差异与提交历史。

## 安全边界（本次新增的部分）

- **操作是白名单枚举，不是"白名单 git 子命令"。** 每个操作由 host 自己拼出参数数组，客户端只能提供分支名、远端名、布尔开关这类标量。原来的写法（客户端给分支名、host 拼一条 `checkout`）在只有 checkout 时是安全的，但把 `merge` / `rebase` / `push` / `branch -D` 都加进来之后，「分支名」会出现在参数数组的第四个、第五个位置，白名单子命令再也兜不住它。
- **分支名/远端名的形状正则收紧**：不含 `..`（挡住路径穿越）、不以 `-`/`.`/`/` 开头（挡住选项注入）、不含 `//`、不以 `/` 结尾。提交只接受十六进制 SHA，**不接受任何 rev 表达式**（`HEAD~1`、`main@{1}` 一律拒绝）。
- **不提供任何改写历史的能力**：没有 `reset --hard`、没有 `--amend`、没有 `push --force`。需要它们的人在终端里做。
- 联网操作单独用 180 秒超时（本地操作 8 秒），否则慢网络下 `push` 会被误杀成"推送失败"。

## 校验

新增五个测试，合计 **657 项断言**，全部在一次性临时仓库上跑真实 git：

| 测试 | 断言 | 覆盖 |
|---|---|---|
| `test-gitbar-branches.mjs` | 153 | 分支详情/分组/同步计算；新建·重命名·删除（含未并入的二次确认）·合并·变基·摘取（含**空摘取**）·fetch/pull/push（本地 bare 仓库，全程离线）；冲突与中止；分支名白名单的 11 种非法输入 |
| `test-gitbar-source-panel.mjs` | 97 | 面板结构、同步标记的三种形状、上游截断、搜索过滤、右键菜单的按上下文禁用、七个对话框的请求体、错误 code → 本地化短句、未知 code 不吞原文 |
| `test-review-graph.mjs` | 103 | 提交图的父提交（**必须是完整 40 位哈希**）、拓扑序、分页不重叠不遗漏、ref 筛选、提交详情、单文件差异、已跟踪/未跟踪分类、暂存/取消暂存/提交 |
| `test-graph-layout.mjs` + `test-graph-layout-parity.mjs` | 184 + 19 | 泳道布局的纯函数（线性/合并/八爪鱼/分叉回收/窗口外父提交/根提交/乱序），以及**内联副本与独立模块逐字段一致** |
| `test-review-graph-view.mjs` | 51 | 主区域三栏视图的两个槽位注册、id 一致、inject 不遮蔽标准钩子、三栏数据、选中取详情、文件差异按需取、按分支筛选、空仓库/非仓库/错误态 |
| `test-review-staging.mjs` | 50 | `classifyEntry` 的四种 porcelain 形状、三组分类、单文件与批量暂存/取消暂存、未跟踪折叠与截断、提交按钮的禁用条件、提交被拒的 code → 短句链路 |


修复过程中被这些测试抓出来的真实缺陷（都不是"看着不对"，而是行为错了）：

- `%(upstream:track)` 在本地远程跟踪引用缺失时给出空字符串，于是界面所有分支的领先/落后恒为 0，而 `git status` 同时显示"落后 1"——同一次刷新里自相矛盾。改为以 `rev-list --left-right --count` 为准。
- 领先/落后曾以 **HEAD** 为基准，于是**非当前分支**的数字全错（实测：与上游齐平的 `develop` 被报成"落后 2"）。改为用该分支自己的完整引用。
- 分支名正则漏了 `..`，`origin/../x` 这种路径穿越能通过校验。
- `%p` **不是**完整哈希（跟随 `core.abbrev`，本机 7 位）。提交图靠父提交哈希匹配同一页里的提交，缩写哈希会连错线；补上 `--no-abbrev`。
- 根提交的改动清单用了 `git diff --root <rev>`，而那个 `--root` 只对"与空树比较"的形式生效——于是它实际比的是"根提交 vs 工作区"，混进了后续提交与工作区的改动。改用 `git show`。
- "在 N 个分支中"把 `--is-ancestor` 的参数顺序写反，导致合并提交只报出一个分支。
- `/commit` 同时被"读提交详情"与"创建提交"占用，于是 `POST /commit` 先命中详情路由的 revision 校验、被当成"缺参数"回 400，提交功能完全不可用。详情路由改名为 `/commit-detail`。
- 摘取一个内容已在当前分支里的提交时，git 会留下"空提交"的中间状态，而界面报成"摘取冲突"。现在提前判空、必要时 `--abort`，回 200 + `empty: true`。
- 签出标签时 `git switch --track v1.0.0` 直接报错（`--track` 只接受分支）。现在按"本地分支 / 远端分支 / 标签或提交"三种形状分别拼参数，标签走 `--detach`。
- **暂存界面把 `call()` 的签名用错了**：本插件的 `call` 是"只发 POST、第二个参数就是请求体"的辅助函数，而暂存界面照 fetch 的习惯写成了 `call(route, { method, headers, body })` —— 于是服务端收到的 `paths` 是 undefined。表现是"点了暂存没反应"，而路由本身完全正常（宿主的 103 项断言当时全绿）。这个坑只有 DOM 层测试能抓到，因此 `test-review-staging.mjs` 里专门断言了每个按钮发出的**请求体**。

同时对客户端做了一处结构性修复：右键菜单与对话框改用固定位置的占位节点，不再从 children 里整个去掉——去掉会让后面的兄弟节点前移一位，React 按位置协调，于是对话框每次「右键菜单关掉」都被当成新组件重建，内部状态（"已确认强删"开关、输入框里已填的名字）全部丢失。对话框另有按实例序号变化的 `key`，否则从「新建分支」切到「重命名分支」时输入框会留着上一次填的名字。

其余既有检查全部通过：`test-plugin-sync`（新增"附属文件 `graph-layout.js` 也要一起同步"的断言）、`check-plugin-i18n`、`check-imports`、`test-i18n`、`test-review-host`、`test-review-overlay-hooks`、`test-gitbar-checkout`、`test-gitbar-workspace`。

`test-unpack` 的插件断言从"只抽查 gitbar 一个"改成**三个内置插件加上附属文件**逐个存在：同步是按目录自动带走的，而"某个插件目录没被 stage 进压缩包"这种错误在只抽查一个时完全看不出来。

---

# 1.3.6

本次修掉「根本没改过的文件也被列成改动」，并按 IDEA 的 Git 工具窗重做了右上角「项目改动」抽屉：宽度可拖、列表与差异都重排了一遍。

## 修复

- **只有文件模式变化的文件不再算改动。** 反馈里的现象是一条 `修改 docker/entrypoint.sh +0 −0`，展开后差异只有两行：

  ```
  old mode 100755
  new mode 100644
  ```

  根因是仓库带着 `core.fileMode=true`（从 Linux 仓库带过来的配置很常见）：Windows 上表达不了可执行位，`git add -A` 于是把 HEAD 里 `100755` 的脚本记成 `100644`，快照树与 HEAD 之间就多出一条"纯元数据"的修改——行数 `0/0`，内容一个字没变。已在临时仓库里完整复现（带该标志时快照树保持 `100755`、差异为空；不带时树变成 `100644`）。

  修法两层：宿主侧所有 git 调用统一带 `-c core.fileMode=false`（从源头不产生这条记录），并在 `describeDiff` 里把 `M` + `0/0` 的条目**连同它的差异片段**一起滤掉（兼容升级前留下的旧索引）。同样的标志也加到了分支徽章的 `git status` 上，否则那里的"改动文件数"也会虚报一个。

- **切项目不再喂错索引。** 项目级请求对所有工作区共用一个临时索引文件（只按会话命名），而索引是与仓库强相关的——切到另一个项目时 git 收到的是一份**别的仓库的索引**。现在索引名里带上工作区路径的哈希。

- **残留索引锁不再让面板永久失效。** 实测踩到：一次超时请求在临时索引上留下了 `.index.lock`，此后每一次快照都失败，面板一直显示那行 git 报错（HTTP 500），只能手工删文件恢复。现在遇到锁会判断年龄，**旧锁清掉后重试一次**（锁目录按 PID 命名、只归本进程使用，旧锁必然是上次被中断留下的），而新鲜锁不动（可能正被另一个 git 持有）。

## 变更

- **抽屉宽度可拖动。** 左边缘手柄：拖动变宽变窄（向左拖是变宽）、双击复位、聚焦后用 ←/→ 微调、Home 复位；宽度按应用记忆，窗口变窄时会自动收进允许区间。手柄是可聚焦的 `separator`，因此不用鼠标也能调。

- **列表与差异参照 IDEA 的 Git 工具窗重排。**
  * 文件行：状态徽标（A/M/D/R + 语义色底）、路径按"目录压暗 + 文件名留亮"分层（长目录先省略，文件名永远完整）、右侧等宽数字的增删行数、悬停整行高亮、行尾箭头指示展开状态；
  * 差异区：独立头部（状态 + 路径 + 增删数）与带行号、增删底色的正文，等宽字体；
  * 提交历史：节点 + 轴线的紧凑排布，短哈希做成色块，作者与日期同行；
  * 面板头部：标题 + 数量徽标 + 「刷新」「收起」两个图标按钮（平时无边框，悬停才出底色）；
  * 空态/错误态从"一行小字"改成带图标的居中提示块。

## 校验

- `scripts/test-review-host.mjs` 扩到 **51 项断言**，新增两组真实 git 场景：**只有模式变化的文件不进列表、差异里也没有它的片段**（同一个文件真改内容时照常列出、行数正确），以及**旧索引锁清掉后请求恢复 200、新鲜锁不会被误删**。
- `scripts/test-review-overlay-hooks.mjs` 扩到 **39 项断言**：新增抽屉部分——宽度手柄存在且是 `separator`、`←`/`→`/`Home` 生效、鼠标拖动 120px 宽度正好增加 120px、拖动期间 `body` 标记与松手后清理；以及 IDEA 式结构（分区标题、两个图标按钮、状态徽标、提交历史条数）与差异行「行号 → 增删标记 → 代码」的子元素顺序契约。
- `scripts/test-diff-readability.mjs` 修好了一处早已失效的取样（它引用了未定义的 `rows`，从没人跑到那一步），改为按插件打的 `data-review-diff-row` 标记取样。
- 其余全部通过：`test-plugin-sync`、`check-plugin-i18n`、`check-imports`、`test-i18n`、`test-version`、`check-version`、`test-unpack`、`test-gitbar-workspace`、`test-gitbar-checkout`，以及中英两版 README 校验。

---

# 1.3.5

本次修复右上角「项目改动」面板**取不到当前会话工作区**的问题：它一直显示"当前工作区不是 git 仓库"，而用户实际在用的项目明明是 git 仓库，切换对话也不动。

## 修复

- **项目级面板现在真正跟随当前对话。** 1.3.3 已经把它改成"读当前会话的 cwd"，但**那一步从未生效**——因为插件在自己的 `inject` 里回传了 `useSessions: ctx.sessions?.useSessions`，而 `sessions` 服务上并没有这个成员：

  * `useSessions` / `useWorkspaces` 是**渲染器按 root 作用域提供的标准钩子**（官方 `dsh-client-ui-session` 用 `slots.provideRoot({ hooks: { sessions } })` 提供 source，渲染器再按 `use${Capitalize<N>}` 把它们绑成 props）；
  * 渲染器合并 props 的顺序是 `{ ...kit, ...injected, ... }`——**inject 会盖掉 kit**，而且不会剔除 `undefined`；
  * 于是注入进去的那个 `undefined` 恰好把标准钩子遮蔽掉，组件里 `typeof useSessions === 'function'` 永远为假。

  结果就是面板只能退到宿主给的 `process.cwd()`（外壳启动目录）。本机上它是 `C:\Users\Administrator`——不是 git 仓库，而用户实际在用的 `F:\code\dshDesktop` 是。于是现象成了"明明有 git 却报不是仓库"，且怎么切换对话都不动。

  现在插件不再注入这两个钩子，标准钩子原样送到：工作区取**当前会话**的 `cwd`，切换对话与新建对话都会自动跟随；只有在没有当前会话（刚打开、未进入任何对话）时才退回宿主工作区。

- **"不是 git 仓库"这句提示会带上项目名**（如「当前工作区（Administrator）不是 git 仓库」）。只显示目录名、不显示完整路径（面板仍然不展示工作区路径），但把一句死胡同提示变成能一眼看出"它看的是哪个目录"——这次排查多花的两轮往返，正是因为从界面上完全看不出这一点。

## 校验

- 新增 `scripts/test-review-overlay-hooks.mjs`（10 项断言，不需要 Electron）：钉住「inject 不得遮蔽标准钩子」，并**复现旧写法的破坏性**——用 `undefined` 盖掉 kit 之后，面板确实退回到外壳工作区；再验证修好后工作区跟随当前会话、切换对话、新建对话三种情形，以及面板仍然没有选择器、正文里没有绝对路径。
- 其余检查全部通过：`test-plugin-sync`、`check-plugin-i18n`、`check-imports`、`test-i18n`、`test-version`、`check-version`、`test-unpack`、`test-gitbar-workspace`、`test-review-host`、`test-gitbar-checkout`，以及中英两版 README 校验。

---

# 1.3.4

本次修复**运行时更新后三个内置插件一起消失**的问题：`latest` 变成 `0.1.5-rc.2` 之后，在应用内更新运行时会让分支徽章、「本轮修改」入口与审查面板全部不见，而且**没有任何报错**。

## 修复

- **内置插件改由外壳在每次启动时同步进当前实际使用的运行时，运行时怎么换都不会丢。** 根因在插件的携带方式：三个插件（gitbar / review / typography）随**本应用**发布，不在 `@deepseek-ai/dsh` 的依赖闭包里，只存在于被打包进安装包的那份运行时里。而运行时可以在应用内被整体替换——新版 dsh 装进 `<userData>/runtime/<版本>/` 后，`current` 联接指向的那份里没有这些包。`server.mjs` 对缺失插件的处理是「找不到就跳过」：既不断言也不报警，于是三个插件一起从界面消失，控制台一句提示都没有。

  现在外壳会在**服务端进程启动之前**，把随包携带的插件（`<resources>/plugins/`）同步进当前实际使用的运行时的 `node_modules/`。一次覆盖三种情形：

  * 运行时更新后自动补齐；
  * **已经装坏的运行时就地修好**——已经更新到 rc.2 的用户不需要回滚或重装，装上 1.3.4 启动一次即可；
  * 外壳升级带来新插件或新版本时，旧 runtime 目录里的旧副本会被刷新。

  同步只做文件拷贝、内容一致时跳过不重写；单个插件失败只记一条警告，不会阻断启动。

- **不再"坏了却不吭声"。** 确实写入了插件时会往 stderr 打一行 `[dsh-desktop] 已同步内置插件 …`；找不到任何插件来源时明确警告「分支徽章与改动审查将不可用」。此前这条路径完全静默，只能靠肉眼发现界面少了东西——这正是本次故障排查费时的原因。

## 变更

- 安装包新增一份 `<resources>/plugins/`（约 164 KB）。它在运行时归档之外**再带一份**散文件，因为它是唯一不依赖运行时的插件来源；不带白名单过滤器，避免将来插件多一个文件时被静默漏掉。

## 校验

- 新增 `scripts/test-plugin-sync.mjs`（40 项断言，无需 Electron）：把"运行时被换掉"精确造出来再修——插件被补齐、再次启动不重写、外壳升级后旧副本被刷新、覆盖时不留下已删除的旧文件、候选顺序会跳过已损坏的运行时、没有任何来源时不乱建目录；并覆盖 `index.ts` 实际调用的那个入口（修复时报告、正常时安静）。测试同时跑过真实仓库的 `plugins/` 与真实的运行时目录布局。
- `npm run typecheck`、`scripts/check-imports.mjs`（20 个文件）、`scripts/check-readme.mjs`（中英两版）、`test-i18n`、`test-version`、`check-version`、`test-unpack`、`test-gitbar-workspace`、`test-review-host` 全部通过。

---

# 1.3.3

本次修复右上角「项目改动」面板**不跟随当前对话**的问题，并按反馈去掉面板里的工作区选择器。

## 修复

- **面板现在跟随当前对话：切换对话与新建对话都会自动切到对应项目。** 此前决定"看哪个项目"的那一环是"倒序扫会话列表、取最近一个带 `cwd` 的会话"，于是：

  * 切回一个更早的、属于别的项目的对话时，面板仍停在上一个新会话的项目上；
  * 新建对话时面板完全不跟着走。

  现改为读**当前会话**（`sessions` 的 `state.current`）的 `cwd`——它就是侧边栏此刻打开的那个会话，新建对话在创建时也会被选中，因此两种情况都能自动跟上。

  取值优先级为：当前会话的 `cwd` → 宿主的工作区（`process.cwd()`）→ 已登记工作区的第一项。后两项只是兜底，只在还没有任何当前会话（刚打开、尚未进入对话）时用到。

  1.3.1 把宿主的 `process.cwd()` 排在首位，但那是**外壳启动时**的工作区，与"用户此刻在哪个对话里工作"是两件事；它因此只适合当作没有当前会话时的兜底。

## 变更

- **面板不再提供工作区选择器，也不再显示工作区路径。** 工作区由当前对话决定；把它做成可编辑，既与"这个面板属于当前对话"的语义冲突，也让选过一次之后不再自动跟随。面板里那个列出绝对路径的下拉框因此整个去掉。

## 校验

- 用桩渲染验证取值链（假 React + 拦截 `fetch`，直接加载插件模块）：当前会话在 projB、而最新会话在 projA 时面板请求的是 projB；切换对话后变为对应项目；新建对话后立刻变为新对话的项目。同一脚本确认面板渲染出的宿主元素里没有 `select`、正文里没有绝对路径。
- `scripts/test-project-panel.mjs` 新增两条断言「面板没有工作区选择器」「不展示工作区路径」，`scripts/probe-project-workspace-choice.mjs` 同步改为核对新的取值链。
- `scripts/check-plugin-i18n.mjs`、`scripts/check-imports.mjs`、`scripts/test-ui-typography.cjs` 全部通过。

---

# 1.3.2

本次重做输入框上方的工具条：分支与「本轮修改」入口从下方的按钮排移到输入框上方，
并给分支切换加上搜索，给审查面板补上可折叠的侧栏行为。（感谢 [@bpbdq](https://github.com/bpbdq) 的贡献。）

## 变更

- **分支徽章与「本轮修改」入口移到输入框上方。** 两者原先挤在输入框工具栏右侧、
  发送按钮左边那一小块地方，分支名一长就被压缩变形。

  现在输入框上方是一整行上下文工具条：左侧是分支徽章（`conversation.input.dock`），
  右侧是本轮修改入口。工具条与输入卡片视觉上连成一体——用负外边距让背景延伸到卡片
  后面、圆角也用同一个 22px，交接处的两个角因此不会露底。

  > 这一排此前放不下控件，是因为扩展点选错了：`conversation.composer.bar` 就是
  > **输入框本体**。往那里注册会顶掉官方注册，表现为界面报 `Failed to load plugins`
  > 或**输入框直接消失**，所以只能退回下方按钮排。本次改由 gitbar 在
  > `conversation.input.dock` 注册一个 `list`/`session` 子槽
  > （`dsh.desktop.composer.actions`），由它渲染整行并给审查插件留位置。

- **分支菜单**：新增搜索框（打开即聚焦并清空、实时过滤）、分支多时先显示「正在加载分支…」、
  搜索无结果时区分「没有匹配的分支」与「没有分支」；当前分支用对勾标出、远程分支标注「远程」；
  菜单会按视口高度自动决定向上还是向下弹出，并把宽度和位置都限制在窗口内；只让结果列表滚动，
  搜索框始终留在顶部。

- **审查面板**：「本轮修改」入口现在显示本轮文件数；点第二次会**收起**侧栏、第三次重新打开，
  收起会保留标签和已展开的差异，方便来回对照。文件列表、状态色、提交历史与差异视图都补上了
  主题变量与等宽字体（按增/删/改分别着色），并补上 `focus-visible` 焦点环和悬停反馈。

- **无障碍**：分支徽章与审查入口补上 `aria-label` / `aria-expanded` / `aria-haspopup` /
  `aria-current`，分支菜单标为 `role="dialog"` 并支持 Esc 关闭后把焦点还给触发按钮。

## 校验

- 类型检查（`npm run typecheck`）通过；相对 import 自洽检查（`scripts/check-imports.mjs`）
  覆盖 19 个文件，通过；界面文案中英对照与占位符检查（`npm run test:i18n`）全部通过。
- `scripts/test-review-sidebar.mjs` 同步改到新的 DOM 标记（概览入口改用 `[data-desktop-review]`
  精确匹配，不再依赖按钮文案），并新增「再次点击收起」「第三次点击重新打开」以及
  「收起保留标签」三条断言；脚本开头先把侧栏从收起状态展开，因此可以反复运行。
- Windows 在本机验证了分支工具条与审查面板的实际渲染；报告问题时的深色底看不清
  （当前分支半透明、`R` 标记 0.55 不透明度）在本版一并改掉。

---

# 1.3.1

本次修复「项目改动」面板识别错项目的问题。

## 修复

- **面板现在自动识别当前项目，不再显示别的仓库。** 此前它可能显示应用自己的仓库而不是你在用的项目。根因是**取值的优先级链断了**：面板挂在全局覆盖层上，该位置不按会话作用域注入 `useSessions`，因此"最近会话的工作区"这一环拿不到值，于是退到候选列表的第一项——那恰好是另一个项目。

  现改为由**宿主直接回答**"当前是哪个工作区"：宿主进程启动时就已经 `chdir` 到了工作区，`process.cwd()` 是唯一不需要推断的来源。客户端只把它作为首选，仍保留手动选择（候选多于一个时显示选择器）作为兜底。

- 宿主返回的当前工作区会**先经安全边界校验**：只有在它本身也属于已登记工作区时才回传，避免客户端拿一个必然被拒的路径去请求。

## 校验

- 宿主测试扩到 **39 项**，新增四条针对"当前工作区"的断言：返回候选列表、候选含本仓库、返回了 `current`、且 `current` 就是本仓库。全部通过。

- 新增 `scripts/probe-project-workspace-choice.mjs`：打印面板取值链上每一环的实际值（决定工作区的那一行、推断顺序、是否调用 `/roots`、宿主的来源），用于自查这类"选错了"的问题——它正是本次定位根因的依据。
