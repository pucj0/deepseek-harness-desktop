# 1.3.8

本次修掉三处"界面上的数字与列表对不上、有时干脆读不出来"的问题，并补上总变动行数与暂存标记。

## 修复

- **大仓库下项目级 git 完全读不出来（HTTP 500）。** 实测 `E:\workspace\mmsm-amis`：该仓库有个 `tmp/` 目录，6,635 个日志文件、37.7 MB，**没有被 `.gitignore` 覆盖**。插件用 `git add -A` 给工作区拍快照，于是 `git diff --unified=3` 要输出 **45.9 MB / 1,062,664 行**，把 `execFile` 的 32 MB `maxBuffer` 撑爆 —— `/review/workspace` 直接 500。界面上的表现极具误导性：**「改动」区块显示 0 个文件／"这个项目当前没有未提交的改动"，而「最近提交」照常列出 20 条**（它走的是另一条路由），于是看起来像"项目级 git 取不到数据"。

  现在：缓冲按改动文件数估算（256 MB 上限、64 MB 下限，只用在体量与文件数成正比的统一差异上）；万一仍然超出，**不再让整条路由失败**，而是文件列表照常返回、差异留空并带 `diffOversized`，界面明确提示原因并建议把那个目录加进 `.gitignore`。单文件提交差异同样放宽。

- **分组与文件列表来自两条路由，可能对不上。** 「已暂存 / 更改 / 未进行版本管理的文件」三个分组此前读 `/review/status`，而下面的文件列表读 `/review/workspace` —— 两次独立请求、各自缓存，只要工作区在途中被切换、或某一边先返回，就会出现"分组里是 A 项目的文件、列表里是 B 项目的文件"。现在**索引态随文件一起在同一次请求里返回**（`staged` / `unstaged` / `untracked`），分组与列表由同一份数据算出，结构上不可能不一致。

- **切换对话 / 工作区后有时永久停在"正在读取差异…"。** 会话标签那条链路（`useChanges`）在"工作区还没解析出来"时只是 `return`，状态永远停在初值 `loading`。现在它进入明确的错误态并说明「当前没有可用的工作区」——与项目级那条链路此前修过的是同一个坑，只是漏改了这一个。

- **切换工作区时短暂（有时长期）显示上一个项目的文件。** 暂存区块现在核对异步结果属于哪一次请求，过期响应直接丢弃；工作区一变就立刻回到加载态，而不是继续显示旧项目的列表。

- **外部数字显示对不上。** 每个文件的行内增删数字来自 `/workspace`，而头部的计数来自文件列表长度；两处若各自计算就会漂移。现在头部同时给出**总变动行数** `+N −M`，与各行求和同源，不可能不一致。

## 新增

- **「改动」区块头部显示总变动行数**（`+N −M`，等宽数字），与每个文件的行内数字同源。
- **文件列表每行显示暂存状态**：`●` 已暂存 / `○` 未暂存 / `?` 未跟踪（悬停给出完整说明）。这是"选择性提交"能不能用的前提——在此之前用户在下面勾了暂存，上面那份列表看不出任何区别，只能靠记忆。行标记 `data-review-row` 现在带**文件路径**（此前是空标记），使"哪个文件已暂存"在 DOM 上可核验。

## 校验

- `test-review-graph` 从 103 项扩到 **112 项**：新增 `/workspace` 兼报 `staged`/`unstaged`/`untracked`，以及**暂存后同一文件变成 `staged: true`、取消暂存后变回 `false`**（用真实的 `git add` / `git restore --staged`）。
- `test-review-staging` 从 50 项扩到 **60 项**：新增文件列表的**总数等于各行之和**、三行各自的暂存标记（yes/no/untracked），以及行标记带路径。
- 新增 `scripts/probe-workspace-bigdiff.mjs`：对真实仓库（默认 `E:\workspace\mmsm-amis`）起一个临时服务端，断言 `/workspace` 返回 200、文件列表非空且标记为截断，并顺带确认 `status`/`history`/`graph` 仍正常。实测 **6,639 个文件、6.7 秒、HTTP 200**（修复前是 101 秒后 500）。
- 其余全部通过：`test-review-graph-view` 51、`test-review-overlay-hooks` 39、`test-graph-layout` 184、`test-graph-layout-parity` 19、`test-i18n`、`check-plugin-i18n`、`check-imports`、`test-plugin-sync`。

## 注意

- 仓库里**未被 `.gitignore` 覆盖的大目录**（如日志目录）会让改动列表被上千个文件淹没。把它加进 `.gitignore` 才是根本解法；本次的截断只保证界面不再因此整体失效。
- 从安装版应用里替换插件文件是**临时**的：应用每次启动都会用随包插件覆盖 `runtime/current/node_modules`（实测确认过）。要长期生效必须走一次发版。

---

# 1.3.7

本次把 git 能力从"一个分支徽章 + 一个改动抽屉"扩到三块：**源代码管理面板**（分支管理、远端操作）、**主区域的提交图**（泳道 + 提交详情），以及对应的宿主路由与测试。三块都按 VS Code 的源代码管理与 Git Graph 扩展的形态做，但每一条操作都落在真实的 git 命令上，并且都有契约测试。

## 新增

- **源代码管理面板**（输入框上方徽章点开，替换原来的"切换分支"列表）：

  * 顶部是搜索框（`搜索分支和操作`）与四个快捷操作：`更新项目…`（fetch + pull）、`提交…`、`推送…`、`新建分支…`、`签出标记或修订…`。
  * 分支按 `最近` / `本地` / `远程` 三段分组。每行从左到右是：当前分支标记、名字、同步标记（`↓N` 落后 / `↑N` 领先 / `↕N M` 已分叉 / 上游已删）、上游分支名（只留分支名，且**保留尾部**——`develop-v7.0.0-yuheng` 与 `…-kexing` 的区别在末尾）、展开箭头。
  * 分支行**右键菜单**：签出、从该分支新建、合并到当前分支、把当前分支变基到它、推送、重命名、删除。菜单项**按上下文禁用**而不是隐藏（当前分支不能签出/合并/删除自己；远程分支不能重命名——本地改名只会换掉跟踪引用的名字，远端纹丝不动）。
  * 七个对话框：新建分支（名字/起点/创建后切换）、重命名、删除确认、合并（可选 `--no-ff`）、变基、签出标记或修订、推送（远端选择 + 无上游时建跟踪）。
  * **删除未并入的分支要两次确认**：第一次不带 `force`，host 用 `git merge-base --is-ancestor` 判定未并入就回 409 `notMerged`，界面上才把"这些提交将无法从界面找回"讲清楚；点过「仍然删除」之后按钮文案变成「仍然删除」，第二次才带 `force`。
  * 合并/变基/摘取**进行中**时面板顶部显示一条提示与「中止」入口，不让用户卡在自己不认识的状态里。

- **主区域的提交图**（侧栏图标打开，占据中间主区域）：

  * 三栏：左侧分支树（`HEAD（当前分支）` / `本地` / `远程` / `标签`，点一行即按该分支筛选）、中间提交列表（多泳道彩色连线 + 提交信息 + 分支/标签徽标）、右侧提交详情（作者/时间/标题/正文、"在 N 个分支中"、改动文件列表，点文件才去取它的差异）。
  * 泳道布局是一个纯数据变换（`lib/graph-layout.js`）：按 `git log --topo-order` 的顺序算出每行的列号与连线。列表做了窗口化渲染，几万条提交的仓库也不会一次渲染几千行。

- **宿主路由**（`dsh-client-ui-gitbar`）：

  * `GET /status`、`GET /branches`（含每条分支的上游、领先/落后、最近提交时间与标题）、
  * `POST /checkout`、`/branch/create`、`/branch/rename`、`/branch/delete`、`/branch/merge`、`/branch/rebase`、`/cherry-pick`、`/remote`（fetch/pull/push）、`/op/abort`。

- **宿主路由**（`dsh-client-ui-review`）：`GET /graph`（分页，带父提交与 refs）、`/commit-detail`、`/commit-file`、`/status`（已跟踪 / 未跟踪分开）、`/untracked`，以及 `POST /stage`、`/unstage`、`/commit`。

- **更改区块的暂存与提交界面**（项目改动抽屉里）：三组文件（`已暂存` / `更改` / `未进行版本管理的文件`）+ 提交信息框。

  * 分组依据是 `git status --porcelain` 的 **XY 两列**（索引态与工作区态），**不是**差异内容——`/workspace` 那条路由给的是"HEAD vs 工作区"的内容差异，它根本不含索引态。因此同一个文件在两边都有改动（`MM`）时会**同时出现在两组里**，这是对的。
  * 每组一个有 `+`/`−` 的批量按钮（全部暂存 / 全部取消暂存）。
  * 未跟踪文件**默认折叠**，并按总数显示（实测一个真实仓库有 6,636 个未跟踪文件，一次列全会让面板变成一堵墙）；展开只列前 20 条并说明还有多少。该组的批量按钮**只对列出的样本生效**——一次 `add` 上万个文件几乎不是用户想要的，而且会让 git 跑很久。
  * 提交框：信息为空或没有暂存内容时按钮禁用，**并在旁边说明为什么禁用**（灰着不给理由，用户只会反复点）。`Ctrl+Enter` 提交；提交失败时**保留**已填信息（失败原因往往与提交信息无关）。提交成功后同时刷新改动差异与提交历史。

## 安全边界（本次新增的部分）

- **操作是白名单枚举，不是"白名单 git 子命令"。** 每个操作由 host 自己拼出参数数组，客户端只能提供分支名、远端名、布尔开关这类标量。原来的写法（客户端给分支名、host 拼一条 `checkout`）在只有 checkout 时是安全的，但把 `merge` / `rebase` / `push` / `branch -D` 都加进来之后，「分支名」会出现在参数数组的第四个、第五个位置，白名单子命令再也兜不住它。
- **分支名/远端名的形状正则收紧**：不含 `..`（挡住路径穿越）、不以 `-`/`.`/`/` 开头（挡住选项注入）、不含 `//`、不以 `/` 结尾。提交只接受十六进制 SHA，**不接受任何 rev 表达式**（`HEAD~1`、`main@{1}` 一律拒绝）。
- **不提供任何改写历史的能力**：没有 `reset --hard`、没有 `--amend`、没有 `push --force`。需要它们的人在终端里做。
- 联网操作单独用 180 秒超时（本地操作 8 秒），否则慢网络下 `push` 会被误杀成"推送失败"。

## 校验

新增五个测试，合计 **657 项断言**，全部在一次性临时仓库上跑真实 git：

| 测试 | 断言 | 覆盖 |
|---|---|---|
| `test-gitbar-branches.mjs` | 153 | 分支详情/分组/同步计算；新建·重命名·删除（含未并入的二次确认）·合并·变基·摘取（含**空摘取**）·fetch/pull/push（本地 bare 仓库，全程离线）；冲突与中止；分支名白名单的 11 种非法输入 |
| `test-gitbar-source-panel.mjs` | 97 | 面板结构、同步标记的三种形状、上游截断、搜索过滤、右键菜单的按上下文禁用、七个对话框的请求体、错误 code → 本地化短句、未知 code 不吞原文 |
| `test-review-graph.mjs` | 103 | 提交图的父提交（**必须是完整 40 位哈希**）、拓扑序、分页不重叠不遗漏、ref 筛选、提交详情、单文件差异、已跟踪/未跟踪分类、暂存/取消暂存/提交 |
| `test-graph-layout.mjs` + `test-graph-layout-parity.mjs` | 184 + 19 | 泳道布局的纯函数（线性/合并/八爪鱼/分叉回收/窗口外父提交/根提交/乱序），以及**内联副本与独立模块逐字段一致** |
| `test-review-graph-view.mjs` | 51 | 主区域三栏视图的两个槽位注册、id 一致、inject 不遮蔽标准钩子、三栏数据、选中取详情、文件差异按需取、按分支筛选、空仓库/非仓库/错误态 |
| `test-review-staging.mjs` | 50 | `classifyEntry` 的四种 porcelain 形状、三组分类、单文件与批量暂存/取消暂存、未跟踪折叠与截断、提交按钮的禁用条件、提交被拒的 code → 短句链路 |


修复过程中被这些测试抓出来的真实缺陷（都不是"看着不对"，而是行为错了）：

- `%(upstream:track)` 在本地远程跟踪引用缺失时给出空字符串，于是界面所有分支的领先/落后恒为 0，而 `git status` 同时显示"落后 1"——同一次刷新里自相矛盾。改为以 `rev-list --left-right --count` 为准。
- 领先/落后曾以 **HEAD** 为基准，于是**非当前分支**的数字全错（实测：与上游齐平的 `develop` 被报成"落后 2"）。改为用该分支自己的完整引用。
- 分支名正则漏了 `..`，`origin/../x` 这种路径穿越能通过校验。
- `%p` **不是**完整哈希（跟随 `core.abbrev`，本机 7 位）。提交图靠父提交哈希匹配同一页里的提交，缩写哈希会连错线；补上 `--no-abbrev`。
- 根提交的改动清单用了 `git diff --root <rev>`，而那个 `--root` 只对"与空树比较"的形式生效——于是它实际比的是"根提交 vs 工作区"，混进了后续提交与工作区的改动。改用 `git show`。
- "在 N 个分支中"把 `--is-ancestor` 的参数顺序写反，导致合并提交只报出一个分支。
- `/commit` 同时被"读提交详情"与"创建提交"占用，于是 `POST /commit` 先命中详情路由的 revision 校验、被当成"缺参数"回 400，提交功能完全不可用。详情路由改名为 `/commit-detail`。
- 摘取一个内容已在当前分支里的提交时，git 会留下"空提交"的中间状态，而界面报成"摘取冲突"。现在提前判空、必要时 `--abort`，回 200 + `empty: true`。
- 签出标签时 `git switch --track v1.0.0` 直接报错（`--track` 只接受分支）。现在按"本地分支 / 远端分支 / 标签或提交"三种形状分别拼参数，标签走 `--detach`。
- **暂存界面把 `call()` 的签名用错了**：本插件的 `call` 是"只发 POST、第二个参数就是请求体"的辅助函数，而暂存界面照 fetch 的习惯写成了 `call(route, { method, headers, body })` —— 于是服务端收到的 `paths` 是 undefined。表现是"点了暂存没反应"，而路由本身完全正常（宿主的 103 项断言当时全绿）。这个坑只有 DOM 层测试能抓到，因此 `test-review-staging.mjs` 里专门断言了每个按钮发出的**请求体**。

同时对客户端做了一处结构性修复：右键菜单与对话框改用固定位置的占位节点，不再从 children 里整个去掉——去掉会让后面的兄弟节点前移一位，React 按位置协调，于是对话框每次「右键菜单关掉」都被当成新组件重建，内部状态（"已确认强删"开关、输入框里已填的名字）全部丢失。对话框另有按实例序号变化的 `key`，否则从「新建分支」切到「重命名分支」时输入框会留着上一次填的名字。

其余既有检查全部通过：`test-plugin-sync`（新增"附属文件 `graph-layout.js` 也要一起同步"的断言）、`check-plugin-i18n`、`check-imports`、`test-i18n`、`test-review-host`、`test-review-overlay-hooks`、`test-gitbar-checkout`、`test-gitbar-workspace`。

`test-unpack` 的插件断言从"只抽查 gitbar 一个"改成**三个内置插件加上附属文件**逐个存在：同步是按目录自动带走的，而"某个插件目录没被 stage 进压缩包"这种错误在只抽查一个时完全看不出来。

---

# 1.3.6

本次修掉「根本没改过的文件也被列成改动」，并按 IDEA 的 Git 工具窗重做了右上角「项目改动」抽屉：宽度可拖、列表与差异都重排了一遍。

## 修复

- **只有文件模式变化的文件不再算改动。** 反馈里的现象是一条 `修改 docker/entrypoint.sh +0 −0`，展开后差异只有两行：

  ```
  old mode 100755
  new mode 100644
  ```

  根因是仓库带着 `core.fileMode=true`（从 Linux 仓库带过来的配置很常见）：Windows 上表达不了可执行位，`git add -A` 于是把 HEAD 里 `100755` 的脚本记成 `100644`，快照树与 HEAD 之间就多出一条"纯元数据"的修改——行数 `0/0`，内容一个字没变。已在临时仓库里完整复现（带该标志时快照树保持 `100755`、差异为空；不带时树变成 `100644`）。

  修法两层：宿主侧所有 git 调用统一带 `-c core.fileMode=false`（从源头不产生这条记录），并在 `describeDiff` 里把 `M` + `0/0` 的条目**连同它的差异片段**一起滤掉（兼容升级前留下的旧索引）。同样的标志也加到了分支徽章的 `git status` 上，否则那里的"改动文件数"也会虚报一个。

- **切项目不再喂错索引。** 项目级请求对所有工作区共用一个临时索引文件（只按会话命名），而索引是与仓库强相关的——切到另一个项目时 git 收到的是一份**别的仓库的索引**。现在索引名里带上工作区路径的哈希。

- **残留索引锁不再让面板永久失效。** 实测踩到：一次超时请求在临时索引上留下了 `.index.lock`，此后每一次快照都失败，面板一直显示那行 git 报错（HTTP 500），只能手工删文件恢复。现在遇到锁会判断年龄，**旧锁清掉后重试一次**（锁目录按 PID 命名、只归本进程使用，旧锁必然是上次被中断留下的），而新鲜锁不动（可能正被另一个 git 持有）。

## 变更

- **抽屉宽度可拖动。** 左边缘手柄：拖动变宽变窄（向左拖是变宽）、双击复位、聚焦后用 ←/→ 微调、Home 复位；宽度按应用记忆，窗口变窄时会自动收进允许区间。手柄是可聚焦的 `separator`，因此不用鼠标也能调。

- **列表与差异参照 IDEA 的 Git 工具窗重排。**
  * 文件行：状态徽标（A/M/D/R + 语义色底）、路径按"目录压暗 + 文件名留亮"分层（长目录先省略，文件名永远完整）、右侧等宽数字的增删行数、悬停整行高亮、行尾箭头指示展开状态；
  * 差异区：独立头部（状态 + 路径 + 增删数）与带行号、增删底色的正文，等宽字体；
  * 提交历史：节点 + 轴线的紧凑排布，短哈希做成色块，作者与日期同行；
  * 面板头部：标题 + 数量徽标 + 「刷新」「收起」两个图标按钮（平时无边框，悬停才出底色）；
  * 空态/错误态从"一行小字"改成带图标的居中提示块。

## 校验

- `scripts/test-review-host.mjs` 扩到 **51 项断言**，新增两组真实 git 场景：**只有模式变化的文件不进列表、差异里也没有它的片段**（同一个文件真改内容时照常列出、行数正确），以及**旧索引锁清掉后请求恢复 200、新鲜锁不会被误删**。
- `scripts/test-review-overlay-hooks.mjs` 扩到 **39 项断言**：新增抽屉部分——宽度手柄存在且是 `separator`、`←`/`→`/`Home` 生效、鼠标拖动 120px 宽度正好增加 120px、拖动期间 `body` 标记与松手后清理；以及 IDEA 式结构（分区标题、两个图标按钮、状态徽标、提交历史条数）与差异行「行号 → 增删标记 → 代码」的子元素顺序契约。
- `scripts/test-diff-readability.mjs` 修好了一处早已失效的取样（它引用了未定义的 `rows`，从没人跑到那一步），改为按插件打的 `data-review-diff-row` 标记取样。
- 其余全部通过：`test-plugin-sync`、`check-plugin-i18n`、`check-imports`、`test-i18n`、`test-version`、`check-version`、`test-unpack`、`test-gitbar-workspace`、`test-gitbar-checkout`，以及中英两版 README 校验。

---

# 1.3.5

本次修复右上角「项目改动」面板**取不到当前会话工作区**的问题：它一直显示"当前工作区不是 git 仓库"，而用户实际在用的项目明明是 git 仓库，切换对话也不动。

## 修复

- **项目级面板现在真正跟随当前对话。** 1.3.3 已经把它改成"读当前会话的 cwd"，但**那一步从未生效**——因为插件在自己的 `inject` 里回传了 `useSessions: ctx.sessions?.useSessions`，而 `sessions` 服务上并没有这个成员：

  * `useSessions` / `useWorkspaces` 是**渲染器按 root 作用域提供的标准钩子**（官方 `dsh-client-ui-session` 用 `slots.provideRoot({ hooks: { sessions } })` 提供 source，渲染器再按 `use${Capitalize<N>}` 把它们绑成 props）；
  * 渲染器合并 props 的顺序是 `{ ...kit, ...injected, ... }`——**inject 会盖掉 kit**，而且不会剔除 `undefined`；
  * 于是注入进去的那个 `undefined` 恰好把标准钩子遮蔽掉，组件里 `typeof useSessions === 'function'` 永远为假。

  结果就是面板只能退到宿主给的 `process.cwd()`（外壳启动目录）。本机上它是 `C:\Users\Administrator`——不是 git 仓库，而用户实际在用的 `F:\code\dshDesktop` 是。于是现象成了"明明有 git 却报不是仓库"，且怎么切换对话都不动。

  现在插件不再注入这两个钩子，标准钩子原样送到：工作区取**当前会话**的 `cwd`，切换对话与新建对话都会自动跟随；只有在没有当前会话（刚打开、未进入任何对话）时才退回宿主工作区。

- **"不是 git 仓库"这句提示会带上项目名**（如「当前工作区（Administrator）不是 git 仓库」）。只显示目录名、不显示完整路径（面板仍然不展示工作区路径），但把一句死胡同提示变成能一眼看出"它看的是哪个目录"——这次排查多花的两轮往返，正是因为从界面上完全看不出这一点。

## 校验

- 新增 `scripts/test-review-overlay-hooks.mjs`（10 项断言，不需要 Electron）：钉住「inject 不得遮蔽标准钩子」，并**复现旧写法的破坏性**——用 `undefined` 盖掉 kit 之后，面板确实退回到外壳工作区；再验证修好后工作区跟随当前会话、切换对话、新建对话三种情形，以及面板仍然没有选择器、正文里没有绝对路径。
- 其余检查全部通过：`test-plugin-sync`、`check-plugin-i18n`、`check-imports`、`test-i18n`、`test-version`、`check-version`、`test-unpack`、`test-gitbar-workspace`、`test-review-host`、`test-gitbar-checkout`，以及中英两版 README 校验。

---

# 1.3.4

本次修复**运行时更新后三个内置插件一起消失**的问题：`latest` 变成 `0.1.5-rc.2` 之后，在应用内更新运行时会让分支徽章、「本轮修改」入口与审查面板全部不见，而且**没有任何报错**。

## 修复

- **内置插件改由外壳在每次启动时同步进当前实际使用的运行时，运行时怎么换都不会丢。** 根因在插件的携带方式：三个插件（gitbar / review / typography）随**本应用**发布，不在 `@deepseek-ai/dsh` 的依赖闭包里，只存在于被打包进安装包的那份运行时里。而运行时可以在应用内被整体替换——新版 dsh 装进 `<userData>/runtime/<版本>/` 后，`current` 联接指向的那份里没有这些包。`server.mjs` 对缺失插件的处理是「找不到就跳过」：既不断言也不报警，于是三个插件一起从界面消失，控制台一句提示都没有。

  现在外壳会在**服务端进程启动之前**，把随包携带的插件（`<resources>/plugins/`）同步进当前实际使用的运行时的 `node_modules/`。一次覆盖三种情形：

  * 运行时更新后自动补齐；
  * **已经装坏的运行时就地修好**——已经更新到 rc.2 的用户不需要回滚或重装，装上 1.3.4 启动一次即可；
  * 外壳升级带来新插件或新版本时，旧 runtime 目录里的旧副本会被刷新。

  同步只做文件拷贝、内容一致时跳过不重写；单个插件失败只记一条警告，不会阻断启动。

- **不再"坏了却不吭声"。** 确实写入了插件时会往 stderr 打一行 `[dsh-desktop] 已同步内置插件 …`；找不到任何插件来源时明确警告「分支徽章与改动审查将不可用」。此前这条路径完全静默，只能靠肉眼发现界面少了东西——这正是本次故障排查费时的原因。

## 变更

- 安装包新增一份 `<resources>/plugins/`（约 164 KB）。它在运行时归档之外**再带一份**散文件，因为它是唯一不依赖运行时的插件来源；不带白名单过滤器，避免将来插件多一个文件时被静默漏掉。

## 校验

- 新增 `scripts/test-plugin-sync.mjs`（40 项断言，无需 Electron）：把"运行时被换掉"精确造出来再修——插件被补齐、再次启动不重写、外壳升级后旧副本被刷新、覆盖时不留下已删除的旧文件、候选顺序会跳过已损坏的运行时、没有任何来源时不乱建目录；并覆盖 `index.ts` 实际调用的那个入口（修复时报告、正常时安静）。测试同时跑过真实仓库的 `plugins/` 与真实的运行时目录布局。
- `npm run typecheck`、`scripts/check-imports.mjs`（20 个文件）、`scripts/check-readme.mjs`（中英两版）、`test-i18n`、`test-version`、`check-version`、`test-unpack`、`test-gitbar-workspace`、`test-review-host` 全部通过。

---

# 1.3.3

本次修复右上角「项目改动」面板**不跟随当前对话**的问题，并按反馈去掉面板里的工作区选择器。

## 修复

- **面板现在跟随当前对话：切换对话与新建对话都会自动切到对应项目。** 此前决定"看哪个项目"的那一环是"倒序扫会话列表、取最近一个带 `cwd` 的会话"，于是：

  * 切回一个更早的、属于别的项目的对话时，面板仍停在上一个新会话的项目上；
  * 新建对话时面板完全不跟着走。

  现改为读**当前会话**（`sessions` 的 `state.current`）的 `cwd`——它就是侧边栏此刻打开的那个会话，新建对话在创建时也会被选中，因此两种情况都能自动跟上。

  取值优先级为：当前会话的 `cwd` → 宿主的工作区（`process.cwd()`）→ 已登记工作区的第一项。后两项只是兜底，只在还没有任何当前会话（刚打开、尚未进入对话）时用到。

  1.3.1 把宿主的 `process.cwd()` 排在首位，但那是**外壳启动时**的工作区，与"用户此刻在哪个对话里工作"是两件事；它因此只适合当作没有当前会话时的兜底。

## 变更

- **面板不再提供工作区选择器，也不再显示工作区路径。** 工作区由当前对话决定；把它做成可编辑，既与"这个面板属于当前对话"的语义冲突，也让选过一次之后不再自动跟随。面板里那个列出绝对路径的下拉框因此整个去掉。

## 校验

- 用桩渲染验证取值链（假 React + 拦截 `fetch`，直接加载插件模块）：当前会话在 projB、而最新会话在 projA 时面板请求的是 projB；切换对话后变为对应项目；新建对话后立刻变为新对话的项目。同一脚本确认面板渲染出的宿主元素里没有 `select`、正文里没有绝对路径。
- `scripts/test-project-panel.mjs` 新增两条断言「面板没有工作区选择器」「不展示工作区路径」，`scripts/probe-project-workspace-choice.mjs` 同步改为核对新的取值链。
- `scripts/check-plugin-i18n.mjs`、`scripts/check-imports.mjs`、`scripts/test-ui-typography.cjs` 全部通过。

---

# 1.3.2

本次重做输入框上方的工具条：分支与「本轮修改」入口从下方的按钮排移到输入框上方，
并给分支切换加上搜索，给审查面板补上可折叠的侧栏行为。（感谢 [@bpbdq](https://github.com/bpbdq) 的贡献。）

## 变更

- **分支徽章与「本轮修改」入口移到输入框上方。** 两者原先挤在输入框工具栏右侧、
  发送按钮左边那一小块地方，分支名一长就被压缩变形。

  现在输入框上方是一整行上下文工具条：左侧是分支徽章（`conversation.input.dock`），
  右侧是本轮修改入口。工具条与输入卡片视觉上连成一体——用负外边距让背景延伸到卡片
  后面、圆角也用同一个 22px，交接处的两个角因此不会露底。

  > 这一排此前放不下控件，是因为扩展点选错了：`conversation.composer.bar` 就是
  > **输入框本体**。往那里注册会顶掉官方注册，表现为界面报 `Failed to load plugins`
  > 或**输入框直接消失**，所以只能退回下方按钮排。本次改由 gitbar 在
  > `conversation.input.dock` 注册一个 `list`/`session` 子槽
  > （`dsh.desktop.composer.actions`），由它渲染整行并给审查插件留位置。

- **分支菜单**：新增搜索框（打开即聚焦并清空、实时过滤）、分支多时先显示「正在加载分支…」、
  搜索无结果时区分「没有匹配的分支」与「没有分支」；当前分支用对勾标出、远程分支标注「远程」；
  菜单会按视口高度自动决定向上还是向下弹出，并把宽度和位置都限制在窗口内；只让结果列表滚动，
  搜索框始终留在顶部。

- **审查面板**：「本轮修改」入口现在显示本轮文件数；点第二次会**收起**侧栏、第三次重新打开，
  收起会保留标签和已展开的差异，方便来回对照。文件列表、状态色、提交历史与差异视图都补上了
  主题变量与等宽字体（按增/删/改分别着色），并补上 `focus-visible` 焦点环和悬停反馈。

- **无障碍**：分支徽章与审查入口补上 `aria-label` / `aria-expanded` / `aria-haspopup` /
  `aria-current`，分支菜单标为 `role="dialog"` 并支持 Esc 关闭后把焦点还给触发按钮。

## 校验

- 类型检查（`npm run typecheck`）通过；相对 import 自洽检查（`scripts/check-imports.mjs`）
  覆盖 19 个文件，通过；界面文案中英对照与占位符检查（`npm run test:i18n`）全部通过。
- `scripts/test-review-sidebar.mjs` 同步改到新的 DOM 标记（概览入口改用 `[data-desktop-review]`
  精确匹配，不再依赖按钮文案），并新增「再次点击收起」「第三次点击重新打开」以及
  「收起保留标签」三条断言；脚本开头先把侧栏从收起状态展开，因此可以反复运行。
- Windows 在本机验证了分支工具条与审查面板的实际渲染；报告问题时的深色底看不清
  （当前分支半透明、`R` 标记 0.55 不透明度）在本版一并改掉。

---

# 1.3.1

本次修复「项目改动」面板识别错项目的问题。

## 修复

- **面板现在自动识别当前项目，不再显示别的仓库。** 此前它可能显示应用自己的仓库而不是你在用的项目。根因是**取值的优先级链断了**：面板挂在全局覆盖层上，该位置不按会话作用域注入 `useSessions`，因此"最近会话的工作区"这一环拿不到值，于是退到候选列表的第一项——那恰好是另一个项目。

  现改为由**宿主直接回答**"当前是哪个工作区"：宿主进程启动时就已经 `chdir` 到了工作区，`process.cwd()` 是唯一不需要推断的来源。客户端只把它作为首选，仍保留手动选择（候选多于一个时显示选择器）作为兜底。

- 宿主返回的当前工作区会**先经安全边界校验**：只有在它本身也属于已登记工作区时才回传，避免客户端拿一个必然被拒的路径去请求。

## 校验

- 宿主测试扩到 **39 项**，新增四条针对"当前工作区"的断言：返回候选列表、候选含本仓库、返回了 `current`、且 `current` 就是本仓库。全部通过。

- 新增 `scripts/probe-project-workspace-choice.mjs`：打印面板取值链上每一环的实际值（决定工作区的那一行、推断顺序、是否调用 `/roots`、宿主的来源），用于自查这类"选错了"的问题——它正是本次定位根因的依据。
